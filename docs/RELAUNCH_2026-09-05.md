# TriplePoker: Three Piles relaunch

## Ad-system refactor follow-up — 2026-09-17

- Added centralized, config-driven interstitial decisioning in `server/src/game/adConfig.ts` and `server/src/game/adManager.ts`. Tier D natural breaks now call the central path; deploy migration `074_tier_d_configurable_ads_rewards_second_deal.sql` before using the new state fields.
- Level-clear rewards are no longer reserved unconditionally: `xx1–xx9` are capped at three random grants per window and `xx0` reserves one rare normal item. Historical grants remain intact.
- Added separate `tier_d_second_deal_inventory` and a recovery state machine. The Daily Streak route awards one stored Second Deal to VIP users completing day 8.
- Remaining release check: connect the visual Second Deal decision and Top 20 auto-reward presentation to the production ad callback before enabling production ads.

## New Architecture LayoutAnimation cleanup — 2026-09-14

- Removed the legacy Android `setLayoutAnimationEnabledExperimental(true)` call
  from the Tier D leaderboard. It is a no-op under React Native New Architecture
  and was the sole source of the development warning; `configureNext` remains.

## Royal Flush VFX card corners — 2026-09-14

- Each Royal Straight Flush VFX card uses the same classic card face asset as
  play, with a proportional 14% rounded clipping frame and no VFX-only border,
  white backing, or outer card glow. The ceremony's separate backdrop glow is
  unchanged.

## Tier D Combo lightning display order — 2026-09-14

- Combo and Super Combo lightning now render in a dedicated topmost overlay
  layer (`zIndex` and Android `elevation` 30000+) so Reveal panels, controls,
  item rails and other table VFX cannot cover the strike or impact flash.
- The overlay remains pointer-transparent and does not block gameplay input.

## Tier D Level-transition label — 2026-09-14

- The temporary Tier D loading screen shown while advancing now reads
  `NEXT LEVEL…` instead of the technical `CONNECTING SOLO…` label.

## Tier D Triple Sweep arc — 2026-09-14

- Tier D no longer opens the full-screen Legendary/Triple Sweep WebP after a
  three-pile sweep. G3 is revealed first and its Player-only "You win" voice is
  allowed to complete before the Triple Sweep title begins.
- `TRIPLE SWEEP` is rendered letter-by-letter along an upward arc in the open
  space above the reveal frame, scales from 0.62 to 1.48, and fades out over
  3.5 seconds. It has no backdrop and does not navigate to or resemble a
  separate screen.
- Audio completion now supports a one-shot callback, with the existing native
  safety expiry and a Tier D fallback protecting progression when Android does
  not report completion. The server keeps controls paused until the arc ends.

## Tier D Level-clear item rewards — 2026-09-14

- Bronze, Silver and Gold (Lv.1–150): one random item; a verified rewarded ad
  adds one more copy (x2 total for that reward).
- Platinum, Diamond and Elite (Lv.151–350): two distinct random items, one
  each; a verified ad adds one further random item.
- Master, Grandmaster and Legend (Lv.351–1000): two distinct random item types,
  each with a random quantity of one or two; a verified ad adds two more items.
- Mythic and later (Lv.1001+): three distinct random items; a verified ad adds
  two more items. Existing VIP/No-Ads entitlement receives that League's ad
  bonus automatically without an ad.
- Migration 062 expands the Level reward ledger into idempotent reward slots;
  the Server owns the band, randomized item selection, quantities and ad claim.
- A cleared Level stops at `CLAIM REWARD`; the rolled items are shown only after
  that explicit claim. VIP receives its automatic bonus there and can then
  continue to the next Level.
- To keep the item economy healthy, ordinary clears no longer grant an item.
  A Level reward is reserved only for a new all-time Longest Streak, a new
  all-time fastest Level clear, or a Level ending in zero. The criteria are
  evaluated by the server before it records the new statistics.
- Persistent inventory is always merged with temporary Match grants in the
  Tier D item rail. Stored Undo is no longer level-gated; it is available
  whenever the current revealed pile is still eligible to undo.

## Tier D Top 20 language flags — 2026-09-14

- Each Top 20 row now places the player's selected-language flag after their
  display name. It supports English, Thai, Simplified Chinese, Japanese,
  Korean, Vietnamese, Indonesian, Spanish, Portuguese and French.
- Each row also shows the player's all-time Tier D Solo `Longest Win Streak`.
  The ranking itself remains ordered solely by cumulative League Points.

## Tier D active-match recovery — 2026-09-14

- Every authoritative Tier D state emission now writes a server-only active-match
  snapshot. A reconnect first restores that snapshot before rendering; it carries
  the deal, staged piles, scores, revealed piles, timer remainder, item state and
  match-only ad item grants.
- The snapshot is deleted only after level settlement. This allows a new server
  process to recover an unfinished level after a restart. Migration
  `065_tier_d_active_match_snapshots.sql` is required before release.
- The client retries its Socket connection with capped exponential backoff until
  it reconnects. A restore failure is shown as an error and must not silently
  start a fresh level; only a confirmed `NOT_FOUND` response starts a new Solo
  level for a player with no active match.
- This is explicitly a language marker, not a claimed nationality. Migration
  063 stores the selected language server-side so other players can see it.

## Tier D League table atmosphere — 2026-09-14

- The existing base felt remains the single table skin. Bronze/Silver add an
  amber identity, Gold/Platinum/Diamond add cool rails and a blue glow, and
  Elite onward adds an amethyst frame, soft animated edge glow and particles.
  This preserves card readability and avoids multiplying large table assets.

## Tier D reward-ledger compatibility — 2026-09-14

- Migration 064 restores the legacy quantity and ad-claim columns when an
  environment has migration 062 but missed its earlier dependency. This fixes
  `REWARD_CLAIM_UNAVAILABLE` caused by a missing `granted_quantity` column.

## Distinct pile and Level win audio — 2026-09-14

- The repeated "You win" was traced to `PILE_WIN` and `MATCH_WIN` both using
  the exact same `sfx_match_win.mp3` source.
- `PILE_WIN` keeps that voice and remains limited to a Player pile victory.
  The successful Level result maps `MATCH_WIN` to the applause-only
  `sfx_congratulation2.mp3` instead, with no second spoken "You win" cue.

## Level-clear result emphasis — 2026-09-14

- The League heading on a successful Tier D result is explicitly centered
  across the full result panel and scales down on narrow screens.
- `LEVEL N CLEARED!` now uses a continuous soft opacity pulse while the result
  remains visible; the trophy sparkle animation continues independently.

## Win Streak table overlay — 2026-09-14

- Win Streak VFX now appears over the fully dealt Tier D table with a translucent
  dim layer instead of replacing it with an opaque black celebration screen.
- The dealt cards, opponents and community piles are visible behind the sun.
  Table controls remain disabled and the arrange timer does not start until the
  three-second overlay finishes and the existing control-ready acknowledgement
  is sent.

## Forgiving Solo AI curve — 2026-09-14

- AI arrangement strength is now capped per League: Bronze/Silver/Gold skill 1,
  Platinum/Diamond skill 2, Elite/Master skill 3, Grandmaster/Legend skill 4,
  and Mythic/Endless skill 5. The previous curve reached skill 10 by Master and
  continued increasing after Level 500.
- Selection is intentionally less exact and now has an explicit per-League
  random pool: Bronze through Mythic use the top 90%, 85%, 80%, 75%, 70%, 65%,
  50%, 40%, 30% and 20% of valid score-ranked arrangements respectively. The
  former windows were 45%, 25%, 10%, 3.5%, 1.2% and narrowed to 0.025% by skill
  10. This preserves legal G1 < G2 < G3 layouts while allowing visible,
  human-like arrangement mistakes.
- Combo specialist strategy weight is reduced from 4 to 1.5. Opponent count,
  timers, Player Missions, pile scoring and Level pass conditions are unchanged,
  so higher Leagues still progress through table pressure rather than near-perfect
  AI recognition.
- Server regression covers all League boundaries, the permanent Mythic ceiling,
  selection-window values, legal bot layouts and Mission/Combo utility. Owner
  device play should now recheck Lv.151 and later League boundaries.

## Arrangement audio recovery — 2026-09-14

- Tier D now explicitly plays card-select/card-move cues on valid arrangement taps.
- Entering arrangement (including Undo, ad return and a fresh deal) stops card
  playback and starts a fresh audio-session activation attempt. Card interaction
  cues bypass critical-effect priority blocking while respecting mute/volume.
- Regression covers interrupted activation, critical effects and mute. Actual
  audible recovery on the owner's Android device still requires a play check.
- Owner logs show AudioPlayer construction failing in Android MediaSession with
  ISession.getController() null. Installed expo-audio creates a MediaSession per
  player. Removed per-sound destruction/recreation: healthy players are retained
  per event and explicitly rewound before playback; invalid players are evicted
  without immediate reconstruction. This reduces session churn, a suspected
  contributor rather than a device-confirmed root cause. All 17 audio regression
  tests pass, including 100 replays with one constructor and mute/background rules.
  Fully close and reopen the native app before device validation to clear the old
  process's audio state; a JavaScript reload alone may not clear native sessions.

## Mission score timing — 2026-09-14

- Pile and Mission score feedback now displays from the authoritative Reveal
  payload. The server already evaluates Mission score/penalty and updates the
  provisional score on Reveal; Commit only finalizes that existing result and
  no longer repeats the score float.

## Tier D required member journey — 2026-09-14

- New authenticated members now enter Tier D Solo after sign-in and profile
  onboarding. The legacy lobby and Tier C+ game routes remain unavailable until
  the server-persisted Bronze milestone is cleared.
- Clearing Level 250 advances Solo progress to Level 251 and unlocks Initiate
  (Tier C). Further Tier D milestones unlock Adept at 501, Mastermind at 701,
  and High Noble at 1001. Token progression is capped by the same Tier D ceiling.
- Existing members whose legacy table ceiling was already above D retain access.
- Migration 061 updates the atomic Solo settlement RPC; it must be applied with
  the other Tier D migrations before this path is released.

## Platinum Lv.151 balance trial — 2026-09-14

- Owner playtesting found repeated failure at Lv.151. The boundary previously
  added a second AI, raised arrangement recognition from skill 3 to 4, increased
  multi-Mission frequency and introduced a Combo-focused AI simultaneously.
- Trial tuning keeps two AI seats and all Player Missions. At Lv.151–160 only
  the Player receives Mission/Combo scoring. AI Mission scoring returns at
  Lv.161; skill remains 3 through Lv.175 and rises to 4 at Lv.176. The dedicated
  Combo specialist now begins at Diamond Lv.201.
- Pile scores, three-Match aggregation, Player Mission probabilities and the
  highest-individual-AI pass comparison are unchanged. Reassess after owner
  device play at Lv.151 before extending or tightening this transition.

## Tier D Lv.1–50 table guidance — 2026-09-14

- An upward-moving, end-credit-style guidance panel occupies the open space
  above the community piles during Lv.1–50.
- Lv.1–10 teaches players to arrange their hand to beat the visible AI piles
  and prepares them for progressively hidden opposition.
- Lv.11–20 introduces the three-Match Solo Level, the highest-individual-AI
  pass comparison, the option to keep climbing Solo, and the Lv.250 Tier C
  unlock milestone.
- Lv.21–30 summarizes the live 4/6/8 pile scores, +5 three-pile sweep,
  independent Mission scoring, Combo/Super Combo ranges, the distinction
  between free Level-based Solo progression and milestone-unlocked advanced
  Tier tables, and the long-term Tier S+ goal of conquering The Last Boss.
- Lv.31–40 introduces the second-app story, TriplePoker: Beyond The Rules, and
  its bridge into The Arena. It promises eligibility for both Solo climbers and
  Tier competitors through a future cross-path selection system for The Last
  Boss challenge. The current Tier S+ Main/Ascendant selection backend must be
  reconciled with this owner-approved Solo/Tier eligibility before release;
  this UI change does not silently alter monthly quotas or settlement.
- Lv.41–50 closes the introduction with an accurate Freemium versus Premium
  summary. Core Solo/Tier progression remains available without a subscription;
  Free play includes ads at defined transitions and optional item rewards.
  Premium refers to the existing VIP/VIP PRO benefits: ad-free play, automatic
  daily Tokens, reward bonuses, exclusive presentation, private tables and
  plan-specific advanced features. The copy transparently links subscription
  and advertising revenue to funding the more varied, layered rules planned for
  the second app. It does not claim a purchase is required for core progression.
  The guidance panel does not appear from Lv.51 onward.
- All messages use the same persisted device-language selection and
  ten-language catalog as Onboarding.

## Third-match table transition (owner approved)

Matches 1 and 2 retain the practice layout. Starting match 3 uses the original
Initiate table image (`table_default.png`) and the existing classic card artwork.
Robin sits opposite the player, shared cards occupy the middle, and the player's
three piles sit below. Points and the single seal retain exactly the same rules.
No token, auction, extra seat, or old server-table lifecycle is activated.

The match number and selected presentation are stored in the active session so
finishing match 2 does not unexpectedly change that match's layout. Old saved
sessions infer their number from completed matches and completion status.
A one-time guide identifies the player cards, scores and seal. Practice/table
switches preserve the draft, committed seal, results and reveal position.
Rival card images and accessibility labels remain hidden until their pile opens.
The guide can be reopened and dismissed with Android Back. Results and hindsight
replays continue to use the existing readable summary/practice screens.

Pre-change copies of the relaunch screen and store are in
`backups/before-table-transition/`; the original project backups below remain intact.

Transition verification: 10 rule/store tests passed, including match-two hand-three,
match-three default, blocked early table entry, view switching and hydration.
Browser QA confirmed the one-time guide, original table/card assets, committing a
seal then switching both ways, scoring the first reveal, and reload resuming the
next unrevealed pile without repeating the guide. Native device touch/layout QA
remains a release check.

Owner authorization: 2026-09-05. Implement option A autonomously, back up first,
retain the old experience for players who understand the deeper rules.

## Backup and recovery

Original HEAD: `cf8a9bb1c0c2ffb1aa0c0efd62d20ff2d3c34b08`.
The working tree was clean before edits.

- `backups/TriplePoker-before-relaunch-2026-09-05.zip`: tracked source and assets,
  verified readable, 902 entries including the original lobby.
- ZIP SHA-256: `7251FF0FDEB74371540A7A2A0426A394C5D9D09FED1014EF013020748D33624B`.
- `backups/TriplePoker-history-before-relaunch-2026-09-05.bundle`: complete Git
  history and refs; `git bundle verify` passed.
- `backups/local-settings-before-relaunch/`: local `.env*` files from client,
  server and auth where present. Private, ignored by Git. Never publish this folder.

Restore into a separate directory by extracting the ZIP, or clone the bundle and
check out the original HEAD. Restore local settings only locally. Dependencies
can be recreated from lockfiles. No database changes or cloud backup were made.

## Launch decisions

- `/` and the old lobby entry now lead to `/launch`, outside authentication.
- `/duel` is a complete on-device solo game against one clearly identified AI
  rival. One rival keeps the nine scoring contests readable for beginners.
- Three hands per match; each hand deals 11 private cards to each player plus
  three pairs of shared cards, all from one shuffled 52-card deck.
- Arrange 3 / 3 / 5 private cards. Evaluate exactly three private cards plus the
  corresponding two shared cards; Pile 3 chooses its best three automatically.
- Require full poker strength Pile 1 <= Pile 2 <= Pile 3, including kickers.
- Each pile win scores 1, each tie scores 0.5 per player. Most total points wins;
  tied matches remain draws, with no arbitrary tie-break or forced overtime.
- Each player has one seal per match. Commit it to a pile before locking. An
  outright win adds 1 point; losses and ties consume it without a bonus. It may
  be saved across hands and expires unused at match end.
- The rival chooses its seal hand before dealing and arranges only its own cards
  and shared cards. No reading player cards, hidden catch-up, or loss balancing.
- Players reveal each pile at their own pace. The visible score includes only
  revealed piles. No countdown, entry fee, ad gate, wallet loss, or daily obligation.
- Legal starter arrangements, three free strategic suggestions, manual tap-to-swap,
  inline foul prevention, and a rules panel support learning.
- Results show each hand and seal outcome. Replay lets players change their own
  arrangement against the same revealed rival plan and original seals. It is
  explicitly hindsight practice, never another scored match or a reward source.

## Persistence and advanced access

One versioned AsyncStorage record holds the active match and local progression.
Drafts, seal selection, locked hands, reveal position and results survive restart.
Writes are serialized. Match completion is idempotent, and is counted when the
player finishes all reveals and opens the result screen. Save & exit pauses the
session; starting a fresh match is offered only on completed results.

Advanced access requires 5 completed matches, 2 seal wins and a four-question
learning check. These are provisional learning thresholds, not calendar gates.
The check explains ordering, shared cards, seal ties and the separate old rules.
Progress is explicitly device-local: it is not an account achievement, paid
entitlement, server security boundary, leaderboard, or a source of tokens.

The original lobby lives at `client/app/(home)/classic-lobby.tsx`. Its old
authentication, token eligibility and server-side authority remain in force.
Home and game route guards prevent accidental early entry into advanced tables.
The previous Grandmaster development preview exception is retained. Advanced
ad-return URLs point to the preserved lobby. Old match exit links return to the
new launch hub. Existing server engines and wallets are untouched.

## Validation and release checks

Automated rule tests cover poker ordering, shared-card selection, reproducible
and distinct deals, legal AI plans, ties, seal scoring/reuse, invalid cards,
three-hand termination, suggested plans and idempotent progression.

Verified during implementation:

- Client and server TypeScript checks passed.
- Both new test suites passed: 9 tests, including actual persistence rehydration
  of a changed draft and reveal position and prevention of duplicate completion.
- Production Expo web export and Android Hermes bundle export succeeded. Android
  export is a JS/assets compilation check, not an installed APK or native device test.
- Browser playthrough: five complete matches, invalid swap blocks locking, seal
  use, fractional tie scores, refresh during reveals, hand transitions, replay,
  fresh match from home, local progress and four-question advanced unlock.
- Incorrect quiz answers stay on the question with an explanation. After passing,
  advanced entry reaches the original sign-in gate; no login or wallet mutation
  was performed during testing.
- A deterministic 200-match simulation (starter plan, seal on Pile 3 of Hand 1)
  produced 87 player wins, 11 draws and 102 rival wins. This is a basic fairness
  diagnostic, not evidence of human retention or a final difficulty target.
- Fixed Expo web middleware `import.meta` parsing through the installed Babel
  preset option; a cache-cleared export verified the fix.
- Web preview adapts its old fixed-height phone frame to smaller viewports.
  Game scores stay visible on scroll and lock/reveal actions remain at the bottom.
- Existing button audio is reused, with a sound toggle accessible before sign-in.

For a local browser preview, run `npm run export:launch`, then
`npm run preview:launch` in `client`, and open `http://localhost:8088/launch`.
For native development, run the existing Expo start command with `--clear` after
the Babel change, then open the project in the normal Android development client.

Before app-store release, run the Android build on real devices to confirm touch,
TalkBack, cold restart, offline startup, back navigation and low-end device timing.
Browser QA does not replace native Android QA. This task changes source and local
previews; it does not publish an app-store build or deploy production services.

Observe voluntary rematch rate, use of different arrangements, and whether players
can explain their next plan. Do not use larger rewards as a substitute for testing
the gameplay. Analytics transport is not added in this implementation.

## Learning localization — 2026-09-06

- Backed up the pre-localization learning screens, shared UI and progression files
  in `backups/before-localization-2026-09-06/` before editing.
- Added English, Thai, Simplified Chinese, Japanese, Korean, Vietnamese,
  Indonesian, Spanish, Portuguese and French for launch, practice/table learning,
  rules, card accessibility labels, results/replay and the strategy quiz.
- Language is detected from browser language preferences, or Intl locale on native.
  Unsupported locales fall back to English. Traditional Chinese is not silently
  treated as Simplified Chinese. Manual selection is saved separately under
  `triplepoker.learning.language.v1`; device-language mode can be restored.
- The selected language continues after the first five matches. Advanced tables
  remain English, with a notice on the learning journey before entering.
- Localization is scoped to learning components and bundled offline. No translation
  service, IP lookup, account change or gameplay/scoring change is involved.
  Thai/CJK line spacing and wrapping accommodate the translated text.
- Validation: client and server TypeScript checks passed; all 15 launch tests
  passed, including ten-column catalog completeness, interpolation, rule/quiz
  coverage, locale fallback and persistence without modifying match progress.
- Web export and Android Hermes export passed. Browser QA verified Thai persistence
  on reload, switching to Japanese mid-match with the same cards/scores, Japanese
  table guide layout, French table/practice switching, and a complete three-hand
  match through the translated results screen. QA used a separate localhost origin.
- Real Android-device testing and native-speaker editorial review remain release
  checks. Export success does not imply an installed Android build was tested.

Country flags for the existing authenticated Ranking were added subsequently;
see `RANKING_COUNTRIES_2026-09-06.md` for behavior, tests and deployment activation.

Audio recovery fixes and QA are documented in `AUDIO_QA_2026-09-06.md`.

## Tier D Solo rework — Prompt 1 (Core Best Five) — 2026-09-12

- Added the reusable server Core Rule `evaluateBestFive()` for deterministic Best
  Five selection from any eligible 5–8 card pool.  It returns the selected five,
  unused cards, category, rank index and score for comparison.
- Added the Tier D Solo G3 adapter `evaluateSoloG3BestFive()`: exactly five
  player cards plus two community cards, with a scalar optional Auction Card.
  Its API and 5–8 card guard prohibit a second Auction Card / Best 5/9+.
- Existing Initiate/Mastermind (Tier C+) state, discard, auction, reveal and
  Call/Fold flows were deliberately not migrated in this prompt. The current
  Solo routes share that legacy implementation and are not an isolated Tier D
  state machine yet; wiring them now would violate the scoped preservation rule.
- Validation: `handEvaluator` + new Best Five tests passed (32 tests); server
  TypeScript build passed. The subsequent Tier D Solo runtime prompt must own
  the new state/deal/reveal integration and remove its manual G3 discard there.

## Tier D Solo rework — Prompt 2 (game loop) — 2026-09-12

- Added an isolated Tier D Solo server loop. It has one human plus 1/2/3 bots
  at levels 1–100/101–400/401+, reusable AI roster/configuration and increasing
  difficulty bands through the endless levels.
- A fresh standard-deck deal is made for G1, G2 and G3; winners receive 2, 3
  and 4 points respectively. Exact poker ties award no points, avoiding an
  arbitrary seat-order winner. G3 retains Call/Fold; folded seats are excluded.
- Progression is explicit: only a level win increments exactly once; a loss
  leaves level unchanged and resets only the current Tier D win streak.
- Added migration 049 with additive defaults and an atomic settlement RPC for
  `tier_d_solo_level`, `tier_d_current_win_streak` and
  `tier_d_best_win_streak`. It does not repurpose legacy solo/daily streak data.
- Validation: targeted Tier D/Core evaluator suites passed (19 tests), and the
  server TypeScript build passed. Tier C+ files were not modified.

## Tier D Solo rework — Prompt 3 (League engine) — 2026-09-12

- Added a server-only, data-driven 50-level League engine: levels 1–20 are
  Relax; 21–50 are Ranked. League points are isolated in a configurable formula,
  reset on a successful final-level clear, and have no loss penalty.
- Definitions reserve finalized state, final rank and award references. No
  existing canonical League lore/names or League medal assets were found in the
  repository, so current unlimited League labels are intentionally neutral
  (`League N`) and asset references remain null pending approved assets.
- No client/table UI was changed. Tier C+ files were not modified.

## Tier D Solo rework — Prompt 4 (Ranked leaderboard/finalization) — 2026-09-12

- Added Tier D-only server leaderboard snapshots. They are available only to a
  ranked player, select same-League nearby-level context, cap display at 20, and
  do not include any population value. The former 100-member launch gate was
  superseded on 2026-09-13 by deterministic mock padding to 20 entries.
- Rank ties resolve deterministically by League Points, level, earliest update,
  then user id. Client input never supplies rank or points.
- Added additive migration 050: private RLS-protected progress/finalization
  tables, snapshot/final-rank indexes, and an advisory-lock, idempotent RPC.
  Clearing a League final level writes final points/rank/time and a history row
  exactly once; ranks 1–3 are trophy-eligible and rank 4+ is not.
- No Tier C+ leaderboard or matchmaking code was changed.

## Tier D Solo rework — Prompt 5 (awards/reward doubles) — 2026-09-12

- Added idempotent Tier D League Medal/Trophy history and consumable item grants.
  Every finalized League receives one medal; trophies are granted only to final
  ranks 1–3 and record the rank. Achievement records never enter x2 logic.
- Consumables are Single Card Swap, Full Redraw and Bomb Defuser. Free claim is
  x1; only a server-verified successful ad or a No-Ads member gives x2.
- Added migration 051 with private RLS tables and service-role-only RPCs. No
  shop or IAP was added, and no Tier C+ code was changed.

## Tier D shared live table integration — 2026-09-12

- Corrected the earlier Prompt 2 note: Tier D has no Call/Fold. Removed the
  remaining Tier D Call/Fold model/action while leaving Tier C+ unchanged.
- Extracted the canonical felt/background/frame into presentation-only
  `SharedGameTable` components. Initiate now consumes the shared surface while
  retaining its existing controller, timer, economy, arrangement and sockets.
- Replaced the temporary Tier D table with an authoritative socket adapter using
  the shared surface, cards, top bar, bot hand row and action styling. Pile
  results now remain visible before the next `tier_d_play` advances the game.
- Automated checks pass: 19 targeted Tier D/Core tests, server build, client
  typecheck and Expo production web export.
- Remaining release check: complete the authenticated Lv.1 -> result -> Lv.2
  browser playtest with an actual test account. The unauthenticated launch button
  correctly routes to sign-in because Tier D progress is server-persisted.

## Tier D arrange/reveal correction — 2026-09-12

- Tier D now deals all eleven player cards and all three two-card community
  piles before the player commits an arrangement. The player swaps cards in the
  shared `PlayerHandView` and submits exactly 3/3/5 cards; this is not a Tier C
  foul check and contains no Call/Fold state.
- G1 and G2 evaluate the selected three cards plus their visible community
  pair. G3 evaluates the selected five cards plus its community pair as Best
  5/7 (or Best 5/8 with the one permitted Auction Card).
- The live adapter shows a single Solo AI seat, reveals each pile comparison and
  winning hand label, then fades the reveal into a completed facedown pile above
  the raised player hand. Targeted Tier D/Core tests, server build and client
  typecheck pass.
- Tier D now stages its deal from a visible vertical 1px-spaced facedown deck
  below the level HUD. Eleven cards fly to the player and six to the community
  area before either is exposed; a completed reveal fades and flies down to the
  completed-pile facedown stack at bottom-right.

## League Gameplay Canon v1.1 — initial implementation audit — 2026-09-13

- Replaced the obsolete 50-level, Relax/Ranked League configuration with the
  canonical named ranges: Bronze through Mythic, with Mythic continuing
  indefinitely after Lv.1500 at its 45-second timer ceiling.
- The initial audit sourced AI count from those canonical ranges and then used
  G1/G2/G3 scores of 2/3/6. The later owner correction to the live 4/6/8
  configuration supersedes those initial values. A tied best pile is awarded
  to the Player; AI-only ties receive no pile.
- Trophy boundaries and Tier C/B/A/A+ unlock boundaries are represented in the
  shared League configuration. No gameplay-screen layout was changed.
- Remaining implementation work is intentionally staged: the current runtime
  is built around three old community-card matches and legacy item identities.
  It still needs the Canon's shared mission engine, negative missions,
  multiplier/combo scoring, Open Challenge, guided Bronze reveals, strict
  arrangement validation, sequential provisional reveal/Undo, timer expiry,
  Canon item inventory migration, and removal of legacy League leaderboard
  finalization from the active League flow. The pasted audit prompt terminates
  during the ×2 item section, so its missing remainder must not be inferred.

## League Gameplay Canon v1.1 — rules foundation and item bar — 2026-09-13

- Added an independently testable League rules module for approved Mission-count
  weights, legal ordered Mission pools, Mandatory Mission+ penalties, Open
  Challenge probabilities/reveal combinations, Mission scoring, multiplier,
  ×2 pile scoring and Combo/Super Combo eligibility.
- Added a five-item bottom-centre item bar using the supplied `Item_Icon.png`.
  The artwork is one unmodified strip; code overlays the inventory quantities
  and disables unavailable controls. The existing persisted items are mapped to
  Shuffle, Swap and Freeze. ×2 and Undo remain visibly disabled at zero until
  their server-authoritative inventory and state-machine support land.
- Validation: 35 targeted server tests pass and the server TypeScript build
  passes. Client `typecheck` is not defined in package scripts; production web
  export was started as the available Metro/TypeScript validation path.

## League Gameplay Canon v1.1 — three-match Level decision — 2026-09-13

- Owner clarification: retain **three Matches per Level**. A Level is cleared
  only after its three Matches conclude and the cumulative Level result is
  resolved. The Canon v1.1 rules apply within each Match; this replaces the
  earlier refactor assumption that one Match must equal one Level.
- The implementation will retain the existing reusable match/table lifecycle,
  while replacing only legacy gameplay rules that conflict with Canon v1.1.
- Final Canon correction: each Match has an independent fresh deck, Missions,
  modifier rolls, AI arrangement and full League timer. A Level pass compares
  cumulative scores across all three Matches; Player passes when their total is
  greater than or equal to the highest AI total, including a tie. Match wins
  themselves never determine Level completion.

## League Gameplay Canon v1.1 — Match scoring integration — 2026-09-13

- Each of the retained three Matches now receives one server-generated shared
  Mission set at deal time. The resolver applies Mission bonuses/negative
  penalties to every participant independently of pile victory, and applies
  Silver+ hand multipliers to pile-win scores. Combo/Super Combo eligibility is
  resolved after G3 for the Match.
- Arrangement submission now rejects invalid `G1 < G2 < G3` arrangements.
  Mission labels are emitted with live Match state and replace the visual pile
  label in the existing central pile area when applicable; no table layout was
  redesigned.
- Validation: server TypeScript build and client Expo production web export
  pass after this integration.

## League Level clear-time record — 2026-09-13

- Added an additive migration and server-authoritative personal-best field for
  Level clear time. The clock starts at Match 1 of a Level, does not reset for
  Match 2/3, and is saved only when the Player passes after Match 3. It records
  wall-clock elapsed time from Level start through clear, as requested.

## League Canon item inventory migration — 2026-09-13

- Added migration 057 to retain existing item quantities while renaming the
  old runtime keys to the Canon keys: Shuffle, Swap, ×2, Freeze and Undo. New
  Level rewards select across all five keys. The active UI and socket protocol
  now use these Canon names; ×2 and Undo remain disabled until their sequential
  reveal state transitions are implemented.

## Open Challenge final pass gate — 2026-09-13

- Owner decision: every Open Challenge Match is a mandatory Level gate. The
  Player must sweep G1/G2/G3 in each such Match and must also meet the normal
  three-Match cumulative-score threshold. A failed sweep makes the Level fail
  even when the Player's total score is highest.

## League Match state machine — Reveal, Undo, ×2 and Timer — 2026-09-13

- Replaced timed client auto-advance with explicit provisional Reveal and
  Commit controls for G1 → G2 → G3 inside each of the three Matches.
- Undo now restores the authoritative pre-reveal score snapshot, reopens only
  the permitted Player piles, keeps committed earlier piles immutable, leaves
  AI arrangements locked, and is limited to G1/G2/G3 once each across the
  whole three-Match Level. Bronze Guided Reveal keeps Undo disabled.
- ×2 binds permanently to a selected unrevealed pile, is consumed immediately,
  survives Undo/Shuffle, and multiplies only that pile's win score. Combo/Super
  Combo randomness moved from provisional G3 Reveal to final G3 Commit so Undo
  cannot reroll it.
- Added an authoritative per-Match countdown using the Canon League duration.
  Freeze stores exact remaining time and resumes on the next screen touch.
  Expiry during Arrange selects the first legal (non-score-optimized) layout;
  expiry during a Reveal decision commits and resolves the remaining piles.
  Bronze remains unlimited and therefore keeps Freeze disabled.
- The Match timer starts only after the blocking Deal animation returns control;
  a Shuffle redeal uses the same pause-and-ack path and cannot consume playable
  time or accept an early socket action.
  AI hands reveal with a short 240 ms stagger; server time and all inputs remain
  paused until the client acknowledges that this system animation has ended.
  Provisional Reveal now includes pile, multiplier, ×2, Mission and penalty
  breakdowns for the Player and every AI.
- Shuffle now preserves the Match Mission, negative penalty, Open Challenge,
  Guided Reveal configuration, elapsed Match timer, and bound ×2 choices while
  replacing the shared deal and locked AI arrangements.
- Validation: 41 targeted League/Solo tests, server TypeScript build and client
  Expo production web export pass.

## League information modifiers, AI strategy and Result — 2026-09-13

- Guided Reveal and Open Challenge now expose only the approved AI piles from
  the arrangement that was already locked server-side. Hidden piles are sent
  as empty slots, so the client cannot recover unapproved AI card identities.
  The existing shared `BossHandRow` renders these face-up/face-down slots
  without changing the approved table layout.
- AI arrangement utility now scales by League skill without reading Player
  cards. Low skill prioritizes legal hand strength, mid skill adds Shared
  Mission value, and high skill also prices Combo/Super Combo expected value
  and Mandatory Mission penalty risk. Shuffle recreates and locks every AI
  arrangement against the preserved Mission/Open configuration.
- The Result panel now uses the Canon League ranges through Mythic/Endless,
  compares the Player against the highest individual AI score, removes the
  obsolete fixed maximum-score claim, and states an Open Challenge sweep-gate
  failure separately from a cumulative-score failure.
- Added the Canon score feedback at every Player/AI Avatar: each non-zero pile,
  Mission, sweep or penalty component floats for about one second with slight
  rotation, scale-up and fade-out. G3 Commit now also presents a shared
  `COMBO!` / `SUPER COMBO!` banner with each participant's awarded bonus.
- Validation: 43 targeted League/Solo tests, server TypeScript build and client
  Expo production web export pass. An authenticated browser/device playthrough
  covering Bronze preview, random Open Challenge, all five Items, timeout and
  three-Match result remains a release check.

## League browser playthrough preparation — 2026-09-13

- Local production preview and the Supabase-connected game server both start
  successfully. Launch progression reaches the League entry point and correctly
  requires a non-anonymous account.
- Fixed web OAuth configuration discovered during the playthrough: web builds
  now detect callback sessions in the URL, return to the current origin's
  `/login` route, and explicitly navigate to the provider URL returned by
  Supabase. Native builds retain `triplepoker://auth/callback`.
- The owner supplied a dedicated email/password test account. The production
  build correctly keeps Dev Login hidden; the development web build exposed
  the test-only form and authenticated successfully through Supabase.
- Completed an authenticated Level 29 browser playthrough across all three
  Matches. Every Match dealt a fresh hand/community, reset to G1, and required
  the full G1 Reveal -> Commit/G2 Reveal -> Commit/G3 Reveal -> Finish cycle.
  Cumulative scores carried across Match boundaries (0-16 after Match 1,
  16-16 after Match 2, and 16-32 after Match 3).
- The observed failure path resolved against the highest individual AI total,
  retained Level 29 without demotion, reset the win streak, and presented
  `TRY AGAIN` with the explicit `RETRY GENERATES 3 NEW MATCHES` message. This
  confirms the browser failure/retry orchestration; an authenticated pass-path
  spot check (including persisted clear time/personal best), random Open
  Challenge, timed League and stocked Item use remain release checks.

## Audio reliability and three-second SFX policy — 2026-09-13

- Audited every bundled audio asset using decoded media duration. BGM remains
  unchanged. Thirteen non-BGM assets exceeded three seconds: Shuffle, legacy
  Match Win, Boss Thunder/Ambience, four Timer/clock textures, three Story
  cues, Tier Unlock and the original Congratulations cue.
- Preserved the original audio tree under the Git-ignored
  `backups/audio_before_3s_2026-09-13/` directory. Trimmed every over-limit
  non-BGM asset to about 2.9 seconds; one-shots receive a short 250 ms fade-out
  while loop textures keep a clean un-faded boundary. A decoded-duration
  verification confirms that every non-BGM asset is now at most three seconds.
- Fixed an intermittent silence path in `AudioManager`: a sound rejected by a
  temporary cooldown/critical-priority gate no longer consumes its dedupe key
  for 30 minutes. One-shot priority blocking and stale-player cleanup are now
  capped by the three-second asset policy plus a one-second cleanup grace.

## League Combo lightning VFX — 2026-09-13

- The authoritative Combo event now includes the Mission pile identities that
  produced the award. When the local Player earns a Combo, blue-white lightning
  strikes those completed card piles in a short stagger, followed by a glowing
  impact ring; Super Combo uses a wider violet-white strike.
- The effect is pointer-transparent, runs alongside the existing Combo banner
  and score float, and does not add delay to G3 Commit or Match completion.
- Validation: server TypeScript build, 30 focused League/Solo tests and client
  Expo production web export pass.

## League Top 20 presentation — 2026-09-13

- Added a dedicated `LEAGUE TOP 20` screen reachable from both pass and retry
  Level Result screens. It uses the existing authenticated Tier D leaderboard
  endpoint, caps the visible board at 20 players, and highlights the current user.
- Ranking is explicitly sorted by cumulative League Points. Sparse boards are
  padded with deterministic mock rivals to exactly 20 entries; mocks exist only
  in the response and are never inserted into player or progression tables.
- The client records the user's rank before the Level and compares it with the
  refreshed rank. Each gained or lost position is animated individually at
  333 ms per position (about three positions per second) and plays one short
  UI tick, so the audible tick count matches the number of ranks traversed.
  Reaching the user's final rank always ends with the Congratulations cue,
  including when the refreshed rank has not changed.
- Validation: focused leaderboard/audio recovery tests (22 passing), client
  Expo production web export, and `git diff --check` pass.
- Remaining release decision: the endpoint still reads the legacy
  `league_points`/`ranked_eligible` progression fields. Canon v1.1 does not yet
  define how League Points are earned under the three-Match Level model, so a
  canonical scoring/persistence rule is still required before real play can
  cause authoritative rank movement.
- The board header now resolves and displays the current League trophy with a
  looping golden aura and sparkle treatment. Rival rows use a green table
  surface; the user's amber/gold highlighted row physically moves through the
  list and flashes brighter at every 333 ms rank step, making the changing
  position visible as well as audible.

## Tier C retained-card Best Five conversion — 2026-09-13

- Removed the Initiate/Tier C manual discard overlay. Ready now locks the full
  arranged 3-3-5 layout, and arrangement timeout submits the same 11 cards.
- Tier C G1/G2 remain exact five-card hands (3 player + 2 community). G3 now
  evaluates Best 5 of 7 (all 5 arranged cards + 2 community), including foul
  comparison, winner resolution, displayed winner rank and best-hand stats.
- The server rejects a Tier C Ready payload unless its 3-3-5 multiset exactly
  matches the 11 cards dealt to that player. A rejection unlocks the client so
  the player can correct and resubmit.
- Added focused Best 5/7 regression coverage. Tier C/Foul tests pass 13/13,
  server TypeScript build passes, and client production web export passes.
- Best 5/6 with an Auction Card fixed as G2 card four belongs to the subsequent
  auction-tier conversion; Tier C has no auction and therefore does not create
  a synthetic Best 5/6 path.

## League table mobile layout refinement — 2026-09-13

- Reduced the five-Item artwork and hit rail from 360×78 to 240×52 (two-thirds
  scale) and moved it to the bottom edge, below the Reveal/Commit action.
- Moved G1/G2/G3 Community piles lower and placed all three in one horizontal
  row with compact spacing. Deal-flight destinations now match that row.
- Increased concealed/revealed AI hand cards by 25% at every bot-count layout
  and raised the AI seats 12 px. The player HUD and error toast were moved above
  the new bottom action/Item stack to avoid overlap.
- Final owner-tuning pass moves the Item rail 10 px upward and the Community row
  another 30 px downward. Every AI card is now the same 50×72 px size as a
  Community card; a tighter overlap keeps multi-AI layouts within the table.
- Client production web export and `git diff --check` pass.

## Tier D 52-card conservation audit — 2026-09-13

- Showdown rendering now preserves the same visible 52-card accounting: each seat's current physical pile moves from its table position into the comparison panel, while the two community cards move only into the winner's row. Losing rows contain only that seat's own physical cards; a tie leaves the community cards in the center. No comparison row creates display-only card copies.

## Tier D empty-item rewarded ad — 2026-09-13

- A Free member who taps any item with zero stock is sent to the Tier D rewarded-ad screen. On verified completion it grants exactly one of that selected item and refreshes the live Match inventory on return.
- The server permits only one successful Free-member ad grant per Match. It reserves the selected zero-stock item while the ad is active, restores the choice if the ad is cancelled or fails, and rejects any second grant even if a client attempts it directly.
- Persistent inventory now contains only Level-reward items. Free ad items and the two random VIP items granted when persistent stock is empty are Match-only; they are consumed first and cleared before the next Match begins.

## Tier D League rest window — 2026-09-13

- The first ten Levels of every League are a non-ranked rest/onboarding window. Top 20 is hidden and the server declines leaderboard snapshots during this window. Competition grouping and ranking begin at the League's Level 11.
- League competition now alternates full `rest 10 → compete 20` windows. A full competition settlement ranks against a padded 20-player snapshot, awards persistent random items (Top 1–3: 3; 4–10: 2; other: 1; VIP +1), clears that round's points, and records a League trophy only for Top 3 while retaining the best rank ever earned.
- Migration `059_tier_d_competition_cycles.sql` is required for competition progress, settlement rewards, and trophy history.
- The Result screen no longer exposes Top 20 from a rest Level merely because the next Level enters competition. Every successful non-final competitive Level instead opens Top 20 automatically after settlement, so the player sees the latest rank/LP movement. The round-final Result shows its settled rank and reward rather than reopening a board whose round points have been cleared.
- Migration `058_tier_d_runtime_item_ad_claims.sql` is required before release. It records provider event IDs so callbacks/retries cannot award an item more than once. The current visual ad adapter is DEV-only; production remains correctly gated until an ad-provider receipt is verified server-side.

- Added an authoritative physical-card invariant covering every dealt hand,
  all six Community cards and the remaining draw pile. It requires exactly 52
  unique identities and runs on fresh Deal/Shuffle, Swap, Arrange, Reveal and
  Undo transitions.
- Verified conservation across the one-, two- and three-AI League bands.
  Arrangements, game-hand projections and reveal Best Five results are views of
  physical cards and are not counted as additional cards.
- Fixed table projection: Completed G1/G2 now contain `seatCount × 3 + 2` cards
  and Completed G3 contains `seatCount × 5 + 2`, instead of the old two-seat
  constants. Deal animation now includes all `seatCount × 11 + 6` dealt cards;
  together with the undealt deck it visibly accounts for 52 during dealing.

## Tier D Triple Sweep celebration — 2026-09-13

- When the local player wins G1, G2 and G3 in one Match, the server evaluates the
  locked G3 outcome immediately after the Reveal G3 action, then holds the card
  reveal while the existing full-screen `LegendaryCardVFX` Triple Sweep presentation
  and its existing celebration sound play. Only after that VFX finishes does G3 open.

## Tier D result and competition polish — 2026-09-13

- Difficulty now rises linearly from 40.0/100 at Level 1 to 98.0/100 at Level 1500;
  the cosmetic IQ estimate uses the same normalized range.
- Competition still follows rest 10 / compete 20, but a League's final partial block
  remains competitive through its final Level (for example, a remaining 10-Level block).
- Clear results place the League name as the largest top heading, followed by its trophy,
  then `LEVEL xx CLEARED!`. A Free member pressing Next Level passes through the existing
  no-reward ad gate before the next Match begins; VIP members proceed directly.

## Tier D item rail hit mapping — 2026-09-13

- Replaced percentage/flex item hit areas with five fixed 48 px absolute targets aligned
  to the 240 px artwork, and made the artwork itself non-interactive. The five actions
  now map in order: Shuffle, Swap, ×2, Freeze, Undo.
- Until the profile finishes loading, the item rail defaults to Free-member behavior,
  so an eligible zero-stock item can immediately open the rewarded-ad flow instead of
  being incorrectly disabled as VIP.

## Tier D two-AI hand layout — 2026-09-13

- At League bands with exactly two AI opponents, each AI remains at the same table Y
  position as before, but its cards are compactly arranged as P1/P2 on the upper row
  and P3 on the lower row. Single-AI and three-AI layouts retain the original row.

## SFX recovery audit — 2026-09-13

- Repeat-Level audio correction: Tier D reveal, pile-win, Triple Sweep and Level-win dedupe keys previously reused level/match/pile numbers across retries. AudioManager retained those keys for 30 minutes, suppressing new-round cues. Keys now include the server's Match start timestamp, plus Undo count for repeated provisional reveals. Existing duplicate-delivery protection remains. This explains retry-related missing cues; it does not establish the cause of all device-wide audio interruptions.

## Tier D physical reveal and foul rule — 2026-09-13

### Item flow correction — 2026-09-13

- Root cause of skipped item ads: the game layout treated `/game/tier-d/ad` as an advanced table, redirecting League-only accounts to Launch. The ad now shares Tier D access requirements. Royal Flush stays open for at least five seconds and then presents `CLAIM +1`; only that press submits the reward request. Successful claims return to the table and refresh stock; failed claims remain visible for retry. Fixed an invalid error-label style reference. Navigation regression exercises the actual layout for League-only accounts and retains the advanced-table guard.

- Owner timer correction: add 15 seconds per Match to every timed League. Bronze remains unlimited; Silver 315s, Gold 195s, Platinum 165s, Diamond 135s, Elite 120s, Master 105s, Grandmaster 90s, Legend 75s, Mythic 60s. Each new Match receives the full duration.

- Follow-up covers all five items with runtime regression tests: receiving an ad reward only adds stock; a separate valid action consumes it. Invalid Swap selection, invalid double-pile target and premature Undo preserve stock. Freeze pauses remaining time; Undo restores the provisional score and arrangement access.
- Swap now preserves every arranged position except the chosen replacement card. Ad grants require a matching reservation, consume that reservation once, and discard unfinished reservations on the next Match. All five runtime tests and server build passed; ad-screen/device interaction still needs visual QA.

- Shuffle now replaces the displayed hand even while the server pauses the timer for the deal animation. Freeze/resume does not reset a locally arranged hand. Server snapshots include the draft layout when returning from an ad.
- The eligible empty Free item is labelled `AD +1`. Its first press opens the existing Royal Flush placeholder for at least five seconds; fulfilment adds one Match-only item, returning focus refreshes inventory, and a separate press consumes it.
- The placeholder explicitly requests development fulfilment even in exported web builds. The server still rejects mock fulfilment in production; real ad-provider integration remains required for release.
- Claims use one stable request ID and server deduplication. Failure stays visible instead of silently navigating back. Item hit areas no longer overlap neighboring icons.
- Server build and the runtime regression covering empty stock, ad reservation/grant, inventory refresh, separate consumption and a changed 11-card Shuffle hand passed. Device visual verification remains.

- Reveal and timer expiry now use the player's actual three piles. Each card move is
  mirrored to the server as a validated physical-card snapshot, so the timeout path
  cannot substitute a favourable automatic arrangement.
- `G1 < G2 < G3` is enforced as a foul result rather than a pre-reveal rejection.
  A fouled seat still reveals its real cards with the red FOULED cross, but is excluded
  from pile wins, Mission effects, Combo/Super Combo, and Triple Sweep rewards.
- The card identity/count validation remains strict: the submitted layout must contain
  each of the eleven dealt cards exactly once.

## Match Win Streak sun — 2026-09-13

## Expo SDK 57 upgrade — 2026-09-14

- Owner requested upgrading for the installed SDK 57 Expo Go. Upgraded sequentially 54 → 55 → 56 → 57; final Expo ~57.0.22, React 19.2.3, React Native 0.86.3, expo-audio ~57.0.5, Reanimated 4.5.1, Worklets 0.10.1, TypeScript ~6.0.3. Dependencies/lockfile aligned using Expo install --fix.
- Recovery copy (ignored by Git): `C:/Dev/TriplePoker/sdk54.bak_20260914/` contains pre-upgrade manifests/config, app and src. Complete original generated Android folder moved to `android-full/` there; regenerated SDK 57 native Android with prebuild. No old source or economy data deleted.
- Removed unused expo-av dependency (no app imports); retained expo-audio. Babel uses react-native-worklets/plugin. Added explicit React Navigation dependencies for the retained legacy AppNavigator, no longer supplied transitively by Expo Router. Active app remains Expo Router.
- Migrated removed StyleSheet.absoluteFillObject to absoluteFill without changing geometry; removed obsolete StatusBar translucent; moved legacy Splash config to expo-splash-screen and added expo-system-ui for dark UI. Fixed pre-existing Initiate discard setters/Ready rejection handler, Image pointerEvents typing, timer type and conditional winner-map typing exposed by full typecheck.
- Passed: Expo Doctor 21/21, client TypeScript, Android Hermes bundle export, web export, native Android prebuild, and 26 audio recovery/item/ad regression tests. Physical Android/Expo Go playback, navigation/ads and VFX still need device QA. Java/adb were not available on PATH, so no APK compilation or device launch was claimed.
- npm audit fix (without force) removed the reported critical/high issues. 17 moderate transitive advisories remain around query-string/decode-uri-component and uuid via Expo/legacy navigation/ngrok/xcode. Do not apply audit --force: npm proposes incompatible downgrades. Reassess upstream patches before release.
- Restart Metro from client with `npx expo start --clear --lan`, reopen SDK 57 Expo Go and scan the fresh QR. Server/SQL unchanged by this upgrade.

- 2026-09-14 Mission Rail: added a thin absolute-positioned rail above unchanged center piles. Server supplies objective labels/rewards/penalties, per-pile pending/provisional/committed outcomes, remaining count and missed/completed Combo messaging. A nonblocking 1.2-second intro appears once after each Match's deal; no input/timer pause is needed. Negative objectives show risk and FAIL penalty. UI does not evaluate hands or scores.
- Commit integration resolves the old G3-only Combo timing conflict: award once when all relevant Mission piles are committed (G2 is possible for a G1/G2 pair); cache the result so later G3 Commit/Undo cannot reroll or pay again. Main rail represents the Player; all seats retain avatar-local Combo feedback. Score floats now use committed score events instead of provisional Reveal. Final G3 feedback remains visible 1.5 seconds before showing next Match/result, without starting the next Match timer early.
- Layout constraint: no card, avatar, score, reveal-control or item-bar position changes. Visual/mobile QA remains: local preview at 127.0.0.1:8088 was unavailable (connection refused). Server build and client web export passed; automated domain/runtime checks cover progression and reward behavior.
- Verification: 56 domain/runtime tests passed, then the expanded scoring + rail subset passed 15 tests (57 unique cases overall). Full client typecheck exposes pre-existing errors in initiate/index.tsx (discard setters/socket) and Tier D item artwork Image.pointerEvents typing; these unrelated issues are not part of the Mission Rail change.

- 2026-09-14 follow-up: sample the Combo-focused AI once at Level-attempt start, preserving the same seat/identity across all three Matches and Shuffle. Next Match passes only the specialist ID into creation, so decks, Missions and other per-Match rolls remain fresh. A new Level attempt rolls the role again. This supersedes the per-Match role selection below.

- 2026-09-14 owner correction: League base points are G1=4/G2=6/G3=8, shared by scoring, AI pile-strength weighting and base-only Win Streak. All AI again earn Mission and Combo/Super Combo bonuses and incur Mission penalties wherever Canon generates Missions; remove the Lv. 1001 eligibility gate (Open Challenge is unchanged). Multi-AI Matches randomly select exactly one Combo-focused AI; others optimize pile strength but retain bonus eligibility. The specialist values Missions/Combo even at lower skills. Single-AI tables retain the skill-based Canon strategy. Shuffle preserves the specialist; each new Match rolls again.

- Win Streak sun now appears only for a positive Match streak. Zero/missing streak skips the VFX and acknowledges control-ready immediately after dealing, without waiting three seconds. Positive streak behavior is unchanged.

- Item audit fixes: Swap updates one card without replaying full-table dealing; a server deal revision triggers Shuffle animation instead. Item-ad navigation waits for a server pause acknowledgement, keeps the Match clock paused through claim, and resumes on return/cancel. Match transitions clear Undo usage. Bronze Free/VIP grants exclude Freeze/Undo; late-pile ad buttons exclude unusable Shuffle/Swap and x2 after G3 reveal. Persistent reward inventory is unchanged.
- Item regression and ad-route tests pass (9 tests), server build and client web export pass. Tests cover ad pause beyond full Match duration, cancellation/claim, separate item use, Undo reuse in Match 2, and Bronze Free/VIP grants. Physical-device ad return/Swap visual QA remains.

- AI Mission participation now begins only at Lv. 1001: Lv. 1–1000 AI receives no Mission bonuses/penalties or Combo/Super Combo, and does not optimize arrangements for Missions. Player Missions remain active. Pile points, hand multipliers, Triple Sweep and three-Match aggregation are unchanged.

- Historical intermediate proposal: Match/Level pile base scores of G1=2,
  G2=3 and G3=4 were considered for both Player and AI. This was superseded by
  the later live 4/6/8 owner correction recorded above; gameplay reads the
  shared League base-score configuration.

- Historical intermediate Streak proposal: G1=2, G2=3 and G3=4. This no longer
  applies; Streak scoring now reads the same live 4/6/8 base-score configuration.

- Match Win Streak compares only won-pile base points (live G1 4, G2 6, G3 8)
  against each AI individually, with ties favoring the Player. It ignores all
  Hand/Item multipliers, Missions, penalties, Combo and Triple Sweep bonuses.
  Level completion still uses full cumulative scores.

- Win Streak counts consecutive Match wins across Levels and sessions, resetting only on a Match loss. The completion payload and result screen now use persisted Match streak, matching the sun VFX. Removed the misleading unconditional reset message on Level failure; the older Level-clear streak remains separate internal progress data.

- Tier D displays a gold sun with rotating rays, sparkling stars and a stationary black match-streak count on a black table overlay for 3 seconds after the initial deal. Reduced-motion preference disables rotation/pulsing.
- Control-ready acknowledgement and Match timer start follow the celebration. Shuffle does not retrigger it, nor does resuming an already-started Match.
- Historical note: Match streak briefly compared final Match scores including
  Combo. The base-only 4/6/8 comparison above supersedes that behavior; ties
  favor the Player and losses reset the streak to zero.
- Two-AI hands use a vertical P1/P2 upper row and P3 lower row. The scoreboard always lists one independent score per seat (Player–AI1–AI2); Level pass compares the Player's three-Match total with the highest individual AI total, never their sum.
- Migration 060 adds persistent match streak storage starting at zero; previous Match wins cannot be inferred from Level streaks. Until applied, the counter survives navigation/Levels in server memory only. Restart the server and reload the client for the new protocol.
- Server build and client web export passed. Device visual QA and applying migration 060 remain.

- 2026-09-14 Reveal recovery: Tier D no longer depends on one client-only acknowledgement to leave the blocking `revealing` phase. The client retries the idempotent reveal acknowledgement once; the server also releases ordinary reveals after a five-second safety deadline and completes a stalled post-G3 Triple Sweep after nine seconds. Starting/replacing a session and finishing a Match clear these independent watchdogs. Regression coverage verifies that a missing first-pile acknowledgement cannot leave Reveal locked.

- 2026-09-14 Deal SFX: Tier D plays one shuffle cue when dealing begins, followed by a card cue for every second animated card. The cadence follows the existing 82ms deal stagger while avoiding an excessively dense sequence; timers are cleaned up when the deal animation is replaced or the screen closes. Pile reveal remains silent except for the Player-win voice.

- Fixed a shared activation promise that could hold all subsequent native sounds indefinitely: activation now expires after 1.5 seconds and pending one-shots have cleanup timers before playback starts.
- Web SFX starts synchronously within the initiating gesture. Interaction recovery now retries pending SFX even on tables without BGM.
- Removed the duplicate RANK_COMPLETE enum declaration that prevented audio tests from type-checking.
- Decoded all 34 unique files referenced by the audio registry with FFmpeg successfully; every non-BGM asset is at most 2.98 seconds. No file replacement was justified by this check.
- Regression coverage includes stalled activation, SFX-only gesture recovery, synchronous web playback, background/foreground changes, and 100 replay cycles. Physical-device sustained playback remains a release check; these tests do not establish the exact cause on the owner's device.

- 2026-09-14 Tier D Door Entry: all initial Tier D routes now pass through a presentation-only Rise door transition, and the result screen overlays the same transition before it requests the next Level. It reuses `game_entrance.png` (which already contains the emblem) and the canonical spade card component/assets for the 10-J-Q-K-A reveal. The real Tier D route remains the only place that resumes or starts socket state. Asset failure immediately enters the real table; reduced-motion uses a short static reveal. Client TypeScript passes; device visual timing remains a release check.

- 2026-09-15 Tier D Door Entry now waits on a player-controlled Next Level button before opening. The waiting view shows the player avatar within the existing honor frame and a personal-best board: fastest Match time, longest win streak and highest one-Match score. Migration 066 adds atomic persistence for the latter after every Match. Run it before expecting a non-zero historic score; it records new Matches only and does not infer old values.

- 2026-09-15 Owner revised Card Burst meaning to successful individual missions, not Combo/Super Combo. Human reveal row blinks then its animation completion starts a 600ms, 16-particle MISSION COMPLETE burst; cards remain visible until the normal next-pile action. Reduced motion uses a short static glow/text. Server projects missionSuccess from existing tierDMissionAwareness (including G3 during Sweep); no scoring/evaluation changes. Reveal identity prevents repeat celebration. Former Combo lightning now mounts alongside the human TripleSweepArc. Physical Android timing/placement and G3 simultaneous Sweep remain visual release checks; server restart is needed for the additional reveal field.

- 2026-09-15 Bronze remains the guided, face-up AI league and earns no Level-clear or Match Win Streak credit. Silver (Lv. 51) begins both counters with the player's first Silver clear/win. Migration 067 updates the authoritative Level-clear RPC; apply it before testing a live backend.

- 2026-09-16 Phase 2 Unified Tier Rules foundation: added `server/src/game/unifiedTierRules.ts` with explicit Tier/game-mode capability resolution, Match-scoped item policy, Tier D xX score-multiplier adapter and future token side-bet shape. Added `sharedCardRules.ts` for reusable 3/3/5, Best-5 and physical-card conservation checks. Tier D now exposes authoritative `gameId`, pre-G1 lock, item eligibility, xX commit and foul-recovery state while retaining legacy socket names (`currentGame`, `matchNumber`, `double_pile`) for compatibility.
- Tier D item actions now require a scoped request (`gameId`, Match index, deal revision and request ID), reject duplicate/stale/forged requests, and commit item state through additive migration 071. Auto Sort uses a legal lightweight arrangement path and remains repeatable by inventory; Swap and Shuffle retain card-conservation behavior while enforcing once-per-Match usage; Shuffle preserves the Match and prior item usage. Freeze stores an item instance duration in the 15–45 second range and resumes automatically from its persisted expiry. Undo is now available only for server-detected foul recovery and keeps the same deal. The client follows server eligibility and sends request scopes.
- Migration 071 is additive: it adds Freeze duration storage, idempotent item receipts and an atomic item/snapshot commit function. The owner confirmed successful application after making trigger creation repeatable with DROP TRIGGER IF EXISTS. No further SQL change was needed for the follow-up below. No economy history or legacy gameplay files were removed. Higher Tier discard, arrangement_2 and auction flows remain untouched.
- Correction to the earlier test diagnosis: current gameplay enables AI Missions from Lv. 161; Phase 2 does not move that threshold to Lv. 1001. Existing Mission fixtures incorrectly assumed a stronger Flush could satisfy an exact Pair/High Card mission. Updated those fixtures to the existing exact-rank rules, preserving negative Mission minimum-rank behavior; all 36 `tierDSolo` cases and Mission awareness regressions pass.
- 2026-09-16 Phase 2 recovery follow-up: the socket verifies the signed-in owner before any Tier D room join/action. The client refreshes authentication on reconnect, disables controls while an item request is pending, retries a lost ACK once with the identical request ID, and requests authoritative recovery on rejection or a second timeout. Snapshot receipts also safely retain special object-key request IDs. Draft layouts require complete, owned 3/3/5 cards before persisting. Freeze UI updates independently of the paused Match timer; committed x2 is shown on its target pile.
- Lifecycle coverage now includes all three Matches, repeated Auto Sort, exact Swap slot/deck return, Shuffle exploits, G1 lock, same-deal foul Undo, concurrent/replayed requests, invalid Freeze/empty deck/invalid draft, and transaction failure both before and after a simulated durable commit. Restart tests restore Freeze after its absolute expiry and deduct only post-expiry time. These persistence tests mock the database; they do not establish live PostgreSQL transaction behavior.
- Compatibility: existing socket names and `currentGame`/`matchNumber`/`double_pile` remain. Mutating item requests now require the additive scope fields; older clients must reload with the new server. A pre-Phase-2 snapshot without item usage is conservatively locked for once-per-Match items until the next Match, rather than guessing unused inventory. Higher Tiers require explicit rule configuration and are not automatically migrated by importing the shared model.
- Regression cleanup: aligned the audio activation timeout fixture with the current five-second timeout (no audio runtime change), and mapped the renamed TriplePoker : Rise learning sentence to its existing ten-language catalog row.
- Release checks still requiring a real environment: Android visual/touch QA for exact Swap placement, x2 target badge, Freeze countdown/background/reconnect and foul Undo; live Supabase item debit/receipt/snapshot atomicity and lost-response recovery using migration 071. Deploy/restart the server and reload the matching client together. No deployment or live item consumption was performed by this verification.
- Final automated verification: server build-typecheck and client TypeScript pass; Expo web export passes. Full regression rerun covered 114 suites / 975 tests: 113 suites / 974 tests passed, with one intermittent VIP Plus privacy-fixture failure. The fixture retained public events from the preceding Game, whose community card could randomly match the new Game's auction card. Clear only that old event history before the fixture begins its new auction; all 37 VIP Plus cases then pass without any production auction change. The separately scheduled High Noble grace-period suite passes all 15 cases. Total unique regression cases checked: 990; the full 114-suite run was not repeated after this test-only fixture correction.
- 2026-09-16 owner-requested Task 1 closure, superseding the incomplete full-run status above: reran the entire regression suite after the last fixture correction with `node node_modules/jest/bin/jest.js --maxWorkers=2 --silent --testPathIgnorePatterns=tests/game/hnGracePeriod.test.ts`: 114/114 suites and 975/975 tests passed (316.18 seconds). Ran the separate High Noble suite with `--runInBand --silent tests/game/hnGracePeriod.test.ts`: 1/1 suite and 15/15 tests passed (16.40 seconds). Combined result: 115 suites / 990 tests passed, no failures. No gameplay code changes were needed. Live Supabase and Android verification remain deferred to the owner after teaching; neither was performed in this run.
- 2026-09-16 Unified Advertising + Daily Streak: central server policy maps existing `none`/`vip`/`vip_pro` values to Free/VIP Pro/VIP Pro Plus. Forced interstitial eligibility is Free-only, one maximum at an approved natural break after five minutes, with a three-minute grace after a completed Rewarded Ad. Pre-table, unlock-celebration and legacy Victory ad routes were removed; Tier D asks the server only at Level completion or chosen retry. The mock provider fails closed in production until a real provider callback is wired.
- Migration 072 is additive and required before this feature is enabled live. It adds policy timestamps, Daily Streak receipts, persistent Heart/Rose social inventory, an atomic eight-day claim RPC and protection-consumption tracking. Daily eligibility remains earned by completing a game on the Bangkok calendar day. Free claims use the Rewarded-Ad mock/provider boundary; VIP Pro and VIP Pro Plus claim directly. Day multipliers are 1/1/2/1/2/1/2/3 and draw normal items independently with weights Undo 5, Shuffle 8, xX 12, Swap 18, Auto Sort 27 and Freeze 30. `vip_pro` (Pro Plus) receives exactly one separate 50/50 Heart/Rose bonus per successful claim; it is never a gameplay item.
- The old 3/5/7 milestone claim endpoint now returns `DAILY_STREAK_REPLACED` so an old client cannot mint a second reward stream. Release checks: apply migration 072 to a non-production Supabase project first, verify the provider callback contract, test Bangkok midnight/membership changes and all social-inventory receipt cases on a real backend, then deploy server and matching client together.
- Follow-up: the existing Tier D rewarded-item entry still uses an empty item slot only to establish one-ad-per-Match eligibility. After a verified completion, the server independently draws from the canonical six-item weighted pool and writes the result to persistent inventory; it never grants a chosen match-only item. Migration 073 makes a retried provider event return its original stored random item, preventing a repeat request from changing the receipt or incrementing inventory. Apply migrations 072 and 073 in order before enabling this path on a live backend.
- Verification for the follow-up: server build-typecheck and client TypeScript pass. The Daily Streak/policy/reward/profile and Tier D item-flow focused Jest set completed without failures. A full-suite rerun after this small persistence-receipt follow-up remains a release check because the preceding full run had one CPU-sensitive `adeptAFK` timeout despite passing when run in isolation.
- 2026-09-16 baseline closure: migrations 072 and 073 were applied by the owner. `server/npm test` completed successfully, including the separately run High Noble grace suite; server build, client TypeScript and Expo web export also pass. This workspace has no server-side Supabase credentials or deployment target configured, so no live RPC transaction, server restart/deployment, authenticated membership switch, or physical-device ad-return test was performed or implied. Keep the mock provider in place until the separately authorized AdMob integration phase.
- 2026-09-16 Android Google Mobile Ads test integration: installed `react-native-google-mobile-ads` (v16.5.0) through its Expo config plugin. Android development/preview EAS profiles explicitly select `google_test`; native config uses Google's official test application ID and provider uses only `TestIds.REWARDED`/`TestIds.INTERSTITIAL`. App initialization/preload is singleton-based, Android code is isolated in `.native.ts`, and web retains a deterministic development mock/fail-closed production path. Reward endpoints receive a test-earned marker only after the native SDK's `EARNED_REWARD` event; a server requires the explicit `AD_PROVIDER_MODE=google_test` staging setting to accept it. This is not SSV and production stays fail-closed. Production App ID is `ADMOB_ANDROID_APP_ID` at native build time; production units are release-gated public build variables. See `docs/ANDROID_GOOGLE_TEST_ADS_CHECKLIST.md`; production also requires UMP/consent and a publicly reachable signed AdMob SSV callback before activation.
- Verification: Android manifest generated by Expo prebuild contains only the official Google test App ID; Android JS bundle and web export pass. Client TypeScript and server build pass. The full server regression command (including its separate High Noble suite) completed successfully after updating the Tier D item-ad test fixture to model the durable reward-route write before inventory refresh. Android native compilation/device test has not been run on this host.
- EAS Gradle compatibility follow-up: v16.5.0 pulled Google Mobile Ads Android 25.4.0, which is reported to require newer Kotlin metadata than Expo SDK 57's Android toolchain. Pin `react-native-google-mobile-ads` to v15.4.0 (Google Mobile Ads 24.3.0) for this SDK 57 test build; prebuild and TypeScript continue to pass. Rebuild with EAS cache cleared and inspect the actual Run gradlew log if it still fails.

## Master audit corrections — 2026-09-18

- The Tier D empty-stock Rewarded-Ad route now has the explicit `SELECTED_ITEM_REFILL` context. It reserves the requested item before the provider callback and fulfills exactly that item after verification; random-item reward routes remain separate. The event receipt remains idempotent and a failed/closed ad grants nothing.
- A Tier D Match timeout now resolves and presents only the pile currently awaiting input. The resolve path clears the active timer, establishes the ordinary reveal pause, and returns control through the existing reveal acknowledgement; G2/G3 receive their own newly armed timers. Regression coverage verifies a G1 expiry cannot cascade into G2 or G3.
- A presentation-only `GREAT JOB!` bridge now reuses `assets/fx/vfx_you_win.webp` for successful Level completion. It holds for roughly 1.7 seconds before the existing Top 20/result decision; failed Levels bypass it. Canonical level result, scoring, rewards and tournament state are unchanged.
- Verification: focused Tier D item-flow/policy Jest tests and the server TypeScript build pass; client `tsc --noEmit` passes. Remaining release check: real-device visual QA of the new Level-clear bridge and native rewarded-ad close/completion behavior.

## Optional How to Play — 2026-09-18

- Removed the mandatory slideshow redirect after profile creation. New and returning players now reach the Tier D entrance door directly; no `onboarding_seen` read/write remains on the normal entry path.
- The existing six-slide tutorial and assets are retained as a repeatable, read-only `HOW TO PLAY` guide from the entrance door. Close, Skip/Close Guide and final slide all return to that door rather than starting a match. Reviewing it does not alter profile, progression, inventory, Tokens, League or streak state.
- The early Solo learn-by-playing guidance and progressive AI reveals are unchanged. Client TypeScript passes; device touch/layout verification of the secondary entrance-door link remains a release check.

## Tier C–A+ post-settlement ads — 2026-09-18

- Added one client exit gateway for settled classic-table navigation. Initiate (C), Adept (B), Mastermind (A) and High Noble (A+) loss/Back-to-Lobby exits may request the new `CLASSIC_POST_SETTLEMENT_EXIT` natural break only after their existing Match-End presentation has completed. The request carries tier, loss outcome and Back-to-Lobby reason; the API rejects a missing/invalid context.
- Wins still use exactly the pre-existing Victory-screen `CLASSIC_GAME_SETTLED` opportunity, now through the same client gateway. No Match-End call was added around that route, so a win cannot double-request a forced ad.
- The centralized server policy remains Free-only, probabilistic/config-driven, cooldown- and Rewarded-grace-protected, session-capped and non-back-to-back. The client serializes repeated taps and aborts a stalled policy request after three seconds, then navigates immediately. No-fill/provider failure similarly continues immediately. Tier S/Grandmaster and Monarch-specific flows are not ad call sites; an A+ Monarch Match can only reach the ordinary settled High Noble exit.
- No Tier C+ Rewarded-Ad reward/competitive-item, Auction, matchmaking or entry-fee behavior was added. Mastermind's Android hardware back is consumed on a settled result so it cannot bypass the Match-End exit gateway. Reconnect/match-not-found/error routes intentionally do not show ads because they do not establish a player-acknowledged settled-result exit. Verification: client TypeScript, server build and focused advertising policy tests pass. Remaining device checks: each loss result's Back-to-Lobby tap and offline/no-fill exit.

## Three-language i18n foundation — 2026-09-18

- The first-release production locale list is now exactly `en`, `th`, and `zh-CN`. The app-wide store persists under `triplepoker.language.v2`, normalizes a prior `zh` choice to `zh-CN`, retains the old learning preference as a one-time migration source, and keeps local selection usable offline. The production selector exposes only the three supported locales.
- `client/src/i18n` establishes semantic-key dictionaries, safe English fallback/interpolation, terminology keys and locale-aware number/date/countdown formatters. Legacy source-string learning catalogs and their other-language rows remain intentionally intact as a compatibility bridge; they are no longer the architecture for new UI.
- The Tier D entrance controls and winner-hand showcase now consume semantic keys. Hand evaluation remains canonical; presentation translates returned rank labels through the shared hand-rank key bridge. The profile-language route accepts explicit `zh-CN` while retaining legacy `zh` compatibility.
- Verification: client TypeScript and the dependency-free `npm run check:i18n` locale guard pass; server build passes. Remaining localization release work is deliberate: extract hard-coded modern Tier D, classic/advanced tables, shop/profile/VFX and user-facing server socket prose incrementally before claiming full app coverage. Android portrait layout/font fallback QA for Thai and Simplified Chinese remains required.

## Tier C+ Best-5 post-Auction arrangement — 2026-09-18

- Owner changed the classic/advanced-table rule: players never select a discard after Auction. Tier C+ keeps Pile 1 at three cards and Pile 3 at five cards (Best 5 from seven with its community cards). Every Auction card is placed in Pile 2; Pile 2 evaluates Best 5 from its actual private-card count plus its community cards. This includes Tier S when multiple Auction cards are awarded.
- Mastermind and High Noble round-two client layouts now start as `3 / remaining Pile 2 / 5`; server validation enforces that shape and rejects an invalid post-Auction arrangement with a code rather than display prose. Legacy discard handlers remain only as compatibility/recovery code and are no longer entered from the normal post-Auction state transition.
- Tier S Arena partition validation, bot arrangement and Final Arrange transition use the same shape. Final Arrange proceeds directly to Final Lock instead of a player discard phase. Existing Joker ante mechanics remain separate from a player-selected discard.
- Verification: server build, client TypeScript, i18n parity (91 keys) and focused Foul/Arena arrangement tests pass. Remaining release check: full interactive device walkthrough of Mastermind, High Noble and Tier S Auction/reconnect/Joker paths before production deployment.

## Swap reward scarcity — 2026-09-18

- Swap is now weight 4/100 in the centralized normal-item pool: Undo 8, Shuffle 14, ×2 16, Swap 4, Auto Sort 26 and Freeze 32. This applies to normal/random item rewards such as Daily Streak and makes Swap the rarest gameplay-item drop.
- Level random rewards now read their configurable weights from `tier_d_item_reward_weights`; Swap is explicitly weight 0 there, so a Level clear cannot increase its availability. The Level 10 Undo milestone remains unchanged.
- The existing empty-stock selected-item Rewarded-Ad path is retained for Swap: the verified provider callback reserves and grants exactly Swap ×1, then returns to the unchanged table and card-selection state. It never invokes Swap automatically. No-fill/failure leaves the reservation restored, grants nothing and resumes usable gameplay.
- Verification: focused Item-flow/policy tests, server build, client TypeScript and i18n parity remain release requirements; live migration application and device rewarded-ad completion/no-fill walkthrough remain required before production.

## Rise shared-button Tier D pilot — 2026-09-18

- Added the native reusable `GameActionButton` and centralized Rise button tokens. The Tier D primary Deal/Reveal/Commit action is the sole production migration in this pilot; no other tier or legacy image-button flow was changed.
- The control uses the approved dark-navy, metallic-gold pill direction with optional leading glyph/divider, size and semantic variant APIs, a restrained shimmer, press-scale/border-glow feedback, light haptics, double-tap protection and saved/system reduced-motion support.
- Verification: `client npx tsc --noEmit` passes. Remaining release check: Android device QA through Deal, G1–G3 reveal, commit and disabled/loading states, including Thai and Chinese font fit, before migrating more Tier D controls or other tiers.

## Rise shared-button Tier D expansion — 2026-09-18

- Extended the Tier D-only pilot to the Open the Door and How to Play entrance controls, Tier D result actions (Try Again, claim, next-level, continue and league link), both selected-item Rewarded-Ad actions, forced-ad continue, podium trophy collection and the League Top 20 close action.
- These are presentation-only replacements: existing callback, reward eligibility, ad verification, disabled/loading state and navigation code remains the source of truth. Small icon-only inventory slots and profile/leaderboard rows intentionally retain their compact controls for this pass.

## Tier D early-level live hand coaching — 2026-09-18

- During Levels 1–5 only, while the existing face-up AI preview is available and the player is arranging cards, the client evaluates each complete pile against every shown AI opponent using a mirror of the canonical Best-5 comparison. A pile that is currently ahead pulses its contributing player cards and displays `G# BEATS AI` immediately.
- The helper is feedback only: it neither commits an arrangement nor determines rewards, fouls, scoring or reveal outcomes. The server remains authoritative. The cue disappears outside the introductory preview window, during reveal, on ties and when any shown AI is not beaten.
- Verification: client TypeScript passes. Remaining release check: device walkthrough of Level 1–5, including G3 Best-5-from-7 and multi-AI preview layouts.

## Tier D entry SVG icon migration — 2026-09-18

- Replaced the entrance screen's cropped `Item_Icon3/4` sprite usage with six native SVG glyphs (profile, crown, cards, flame, trophy and chart). The vertical icon rail now uses a fixed 48×48 SVG viewBox inside equal-size containers, so image-crop offsets cannot disturb alignment.
- Added the Expo SDK-compatible `react-native-svg` dependency. Existing entry actions for avatar editing, personal best and streak records are unchanged.
- Verification: client TypeScript passes. Remaining release check: rebuild the Android development client after the new native dependency, then verify the 6-icon rail and compact profile/record icons on portrait devices.

## Tier D item-rail clearance — 2026-09-18

- Reduced the in-table Tier D item rail and its sprite tiles uniformly by 10% (48px to 43.2px) so the six-item strip clears the Rise primary action button. Item behavior, labels and press targets remain unchanged.

## Tier D winning-hand showcase correction — 2026-09-18

- Corrected the flying/flip winning-hand presentation to use the server-returned canonical `bestFive` cards, rather than the first three private cards. The showcase now includes only community cards that belong to that Best Five and the corresponding winning private cards, including G3 Best-5-from-7 outcomes.
- This is display-only; server evaluation, score and settlement remain unchanged. Client TypeScript passes. Remaining release check: device verify a G3 hand whose Best Five excludes one or both community cards.

## Tier D League leaderboard runtime safety — 2026-09-18

- Removed the optional native `Animated` trophy/rank pulse from the Tier D League leaderboard after device runtime reported that `Animated` was unavailable at component creation. The league data, rank progression, auto-complete timer, close action and all leaderboard navigation remain unchanged.
- The trophy and player-rank emphasis are now static styling, eliminating the `Animated.Value` construction path that crashed the screen. Verification: client TypeScript and whitespace/diff checks pass. Remaining release check: open the leaderboard from a Tier D settlement on a rebuilt development client.
- The component no longer directly renders React Native `Image` either, after an Android runtime reported `Image` as unavailable despite the source import. Its league trophy uses a gold glyph and remote avatar URLs fall back to an initial; configured preset avatars remain supported. This isolates the leaderboard from the failing direct `Image` view path while keeping ranking data and navigation intact.

## Tier D table top controls SVG migration — 2026-09-18

- Replaced the Profile and Return-to-Lobby icons in the Tier D table top bar with fixed-size native SVG profile and home glyphs. The old cropped `Item_Icon3` sprite is no longer used by those controls, while their touch targets and existing navigation callbacks are unchanged.
- Verification: client TypeScript and diff whitespace checks pass. This requires the same rebuilt Android development client as the other `react-native-svg` UI work.

## Tier D winner-hand reveal readability — 2026-09-18

- The pre-reveal floating cards now render with the same `card_back_default.png` asset used by the game table, instead of the generic logo-style `Card` back. The winning Hand Ranking fades into the open space above the card area 200ms before any card begins flipping.
- Rank labels now use an undecorated normal-weight presentation for every opponent result. The gold top/bottom lines and backing plate appear only when the local player wins that pile; combo labels retain their existing treatment. Verification: client TypeScript and diff whitespace checks pass.

## Rise backlog batches 1–3 follow-up — 2026-09-19

- Added regression coverage for the Tier D manual-Reveal timer race and the selected-item Rewarded-Ad reservation. A manual Reveal can now be verified not to allow its old timer to reveal a later pile; a selected-item reservation is pinned, rejects a stale different-item callback, is restored on failure and cannot be granted twice. The existing server-side runtime remains the canonical owner of the timer and item state.
- Added the configurable `TRIPLE_SWEEP_THUNDER` AudioManager event, reusing the bundled electric-crack source with a distinct volume/cooldown policy. It plays once at the early lightning peak of the local Triple Sweep VFX; it does not affect scoring, settlement or server state.
- Entrance Door Personal Best labels now use semantic EN/TH/zh-CN keys. The three statistic rows use a fixed numeric column, tabular figures and a common baseline for reliable alignment across release languages.
- Batch 3 asset dependency remains open: the six approved Profile/Lobby/Top 20/Streak/PB/Showcase assets are not yet present in the repository, so they were not substituted with invented artwork. Android portrait-device QA remains required for the new multilingual labels, flip/reveal sequence and Triple Sweep thunder timing.
- Verification: focused `tierDItemFlow` Jest suite (13 tests), `npm run check:i18n` (95 keys), client `npx tsc --noEmit`, and server build pass.

## Tier C+ Door Opening decision — 2026-09-19

- Owner removed the proposed Tier C+ Door Opening VFX from scope. Preserve the existing matchmaking → table → deal sequence; do not add a ready-room delay, presentation gate or reconnect-specific Door flow. This avoids changing the established authoritative start/reconnect protocol.

## Tier D VIP post-Level analysis — 2026-09-19

- Added a volatile three-match analysis buffer for the Tier D Level Result flow. The server enumerates only the player's already-dealt cards with the canonical evaluator, legal G1 < G2 < G3 rule, Mission, Combo and pile scoring. It reads an opponent layout only when every pile was already visible before Reveal; hidden cards and post-Reveal AI hand results are excluded. It sends no opponent cards, draw-pile cards, or hypothetical Auction/Swap outcomes to the client.
- VIP players can open one compact two-tab analysis screen from Level Result. Free players receive score-only teaser eligibility at a total missed score of +6 or more, followed by the required 50/50 local roll. The buffer is cleared when continuing to the next Level; no database, AsyncStorage, profile or history record was added.
- Verification: focused `tierDAnalysis` Jest suite (5 tests), server TypeScript build, and client `npx tsc --noEmit` pass. Remaining release check: portrait-device QA of the Result → VIP Analysis → Next Level flow.

## Tier D Top 20 interstitial — 2026-09-19

- A Free member now enters the existing forced-interstitial flow every time an eligible post-Level Top 20 screen would open. On return from the ad, the pending Top 20 panel opens. VIP members remain direct-entry. Levels without an eligible Top 20 panel keep the existing natural-break ad policy unchanged.
- Verification: client `npx tsc --noEmit` passes. Remaining release check: validate an available and unavailable native interstitial on a portrait device, including return to Top 20.

## Personal Top 20 tournament model — 2026-09-19

- Replaced shared per-League mini-tournament enrollment with one private 24-hour event owned by each player. Five or six cleared non-competition Levels (stable per player/cycle) trigger the event. While an event is open, further Solo clears add canonical settlement points without advancing the next trigger counter.
- Each event snapshots 20–40 recently active real rivals near the owner's Level when enough server players are available, plus 20 deterministic mock rivals. Mock scores advance in deterministic 15-minute ticks at a slower rate than sustained real play. Only the owner can read the private board or receive its Top 20 reward; real rivals cannot receive rewards from another player's event.
- The Top 20 screen now displays an `HHH MM` countdown and refreshes standings every 30 seconds. Free members use the required interstitial before each automatic post-Level visit while the event is active; VIP members enter directly. League-competition Levels pause trigger accumulation but an already-open personal event remains visible and continues accepting scores.
- Added migration `081_tier_d_personal_mini_tournaments.sql`. Verification: focused mini-tournament Jest suite (9 tests), server build, and client TypeScript check pass. Remaining release checks: apply migrations 077–081 and validate creation/countdown/ad/lock/reward on a staging database and portrait device.

## Tier D Reveal control and showcase placement — 2026-09-19

- Removed the play-triangle glyph and divider from the Tier D Reveal/Commit action so the shared button presents text only. Moved the assembled winner-hand flip showcase upward by 68px beyond its previous offset, with the hand-rank label moved in concert, so the card bottoms clear the Mission rail.
- Verification: client `npx tsc --noEmit` passes. Remaining release check: confirm Mission-rail clearance across supported portrait aspect ratios.

## Door Entry personal-stat alignment — 2026-09-19

- Removed the flame icon before Longest Streak in the Door Entry personal-stat board. Fastest Time, Longest Streak and Best Match Score now share fixed 96px label and 32px right-aligned value columns, so their numeric edge remains aligned across locales.
- Verification: client `npx tsc --noEmit` passes.

## Tier D fresh entry deal — 2026-09-19

- A newly mounted Tier D entry now starts a fresh authoritative Solo deal instead of requesting a persisted active-match snapshot. Reconnecting within the same mounted Match still resumes the live session, preserving normal network resilience; reopening the experience after Door Entry does not replay an old cached layout.
- Verification: client `npx tsc --noEmit` passes.

## Tier D Reveal action text wrapping — 2026-09-19

- Long Tier D Reveal/Commit labels may now use two lines. Automatic font shrinking is disabled for this action, retaining the standard large-button 14px text size used by `REVEAL G1`.
- Verification: client `npx tsc --noEmit` passes.

## Tier D Reveal timer handoff — 2026-09-19

- Pressing Reveal now freezes the client-visible timer immediately and suppresses the final five-second `TIME!` overlay for the entire reveal/commit transition. The frozen clock is released only when the next pile receives its new authoritative timer; a rejected request restores the normal clock.
- Verification: client `npx tsc --noEmit` passes.

## Tier D Perfect Play placement — 2026-09-19

- Moved the Free-member `PERFECT PLAY FOUND` teaser out of the Level Cleared result screen. It is now a tappable card above the `GREAT JOB!` win animation, showing the missed-point summary and taking the player to the VIP Analysis upgrade path. The celebration remains visible longer when this card is present so it can be read and tapped.
- Verification: client `npx tsc --noEmit` passes.

## Tier D VIP Analysis return path — 2026-09-19

- VIP Analysis now has `BACK TO LEVEL CLEARED`, returning to the ordinary post-Level result screen with its existing rewards, navigation and Next Level actions intact. `NEXT LEVEL` remains available directly from analysis.
- Verification: client `npx tsc --noEmit` passes.

## Tier D Door cadence — 2026-09-19

- The full Open the Door entry remains the first experience on entering Tier D. After a Level, it now appears only when entering Level 10, 20, 30 and subsequent multiples of ten; all other next-Level transitions go directly to the existing loading taunt before the deal. This decision is preserved across a forced post-result ad.
- Verification: client `npx tsc --noEmit` passes.

## Tier D analysis action sizing — 2026-09-19

- Added an opt-in content-width mode to the shared Rise action button. The VIP Analysis return and next-Level actions now size to their labels instead of using the page-width button frame, preserving the complete fixed-size button text.
- Verification: client `npx tsc --noEmit` passes.

## Rise button content sizing — 2026-09-19

- Content-width sizing is now the default for shared Rise action buttons, while callers with an explicit width or stretch style retain that deliberate layout. Tier D's central action no longer has a fixed 142px frame; its Commit/Reveal transition uses an explicit two-line label and the button measures to its longest line at the standard 14px font size.
- Verification: client `npx tsc --noEmit` passes.

## Tier D winner hand-rank alignment — 2026-09-19

- Moved the winner Hand Ranking label to the same fixed top coordinate as the AI score line, aligning its upper edge with the left and right AI score presentation during the winner-hand showcase.
- Verification: client `npx tsc --noEmit` passes.

## Tier D completed-pile placement — 2026-09-19

- Shifted the completed-pile stacks in the lower-right table area 20px right and 20px down, retaining their existing stagger and count labels.
- Verification: client `npx tsc --noEmit` passes.

## Tier D AI preset avatars — 2026-09-19

- Replaced the Tier D table's emoji-only AI avatar rendering with the existing image-backed avatar presets. Each bot deterministically selects one of the bundled image presets from its stable bot id, so its appearance does not change during a Match.
- Verification: client `npx tsc --noEmit` passes.

## Personal Top 20 board population — 2026-09-19

- The personal Top 20 API now returns display names for real entrants and distinct numbered names for mock rivals. It also provides an owner-plus-20-mock read fallback for legacy events whose entry rows are absent, so the private board never renders as an empty list while normal canonical events retain their stored standings.
- Verification: server build and client `npx tsc --noEmit` pass.

## Rise Solo Lv.1000+ AI personalities — 2026-09-23

- Scoped personality-aware arrangement selection to Tier D/Rise Solo at Lv.1000 and above. Lv.1-999 continues through the original scorer and candidate window; multiplayer AI configuration and engines are unchanged.
- Reused the existing legal-candidate enumeration, shared hand evaluator, Mission/Combo utility and difficulty selection. A final weighting layer now gives Reaper G3/ceiling/Combo appetite, Crag weak-pile protection and balance, and Cypher public-board adaptation. Cypher has an optional auction input that callers may supply only after that card is visible to the AI; the current initial-arrangement flow does not supply a future auction card.
- Centralized all personality tuning and the bounded `.20 -> .12` endless-Level candidate-window ramp in `server/src/game/tierDRiseSoloPersonality.ts`. The non-zero floor intentionally preserves a practical, beatable skill ceiling.
- Development-only logs identify personality, principal reason, chosen ranks and candidate-pool size. No personality reasoning is included in client payloads.
- Added focused regression tests for the Lv.1000 gate, unchanged legacy and multiplayer configs, persistent personality separation, bounded difficulty ramp, visible auction adaptation and hidden-card independence.
- Verification: server TypeScript build passes; focused new + existing Tier D suites pass 42/42. A full `npm test` run was stopped after the long-running Arena workers continued without a final summary; before stopping it reported three unrelated failures in pre-existing/dirty areas (`hnSnapshot`, `launchLanguageStore`, and an `adeptAFK` timeout), while both `tierDRiseSoloPersonality` and `tierDSolo` passed in that same run. Remaining release check: play-test Lv.1000+ on device to tune the centralized weights against the intended 85-90% practical ceiling and rerun the full suite after the three baseline failures are resolved.
