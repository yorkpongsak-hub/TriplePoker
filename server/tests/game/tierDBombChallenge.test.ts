import { advanceTierDBomb, createTierDBomb, defuseTierDBomb, isTierDBombEligible, setTierDBombInteraction } from '../../src/game/tierDBombChallenge'
describe('Tier D Bomb Challenge', () => {
  test('normal levels are inactive and configuration controls eligibility', () => { expect(isTierDBombEligible(9)).toBe(false); expect(isTierDBombEligible(10)).toBe(true); expect(createTierDBomb(11).status).toBe('inactive') })
  test('only interaction time counts; bot thinking, animations and background pause it', () => { let bomb=createTierDBomb(10,{ ...require('../../src/game/tierDBombChallenge').tierDBombConfig,durationMs:10000 }); bomb=setTierDBombInteraction(bomb,true,0); bomb=advanceTierDBomb(bomb,3000); bomb=setTierDBombInteraction(bomb,false,3000); bomb=advanceTierDBomb(bomb,9000); expect(bomb.remainingMs).toBe(7000); bomb=setTierDBombInteraction(bomb,true,9000); bomb=advanceTierDBomb(bomb,12000); expect(bomb.remainingMs).toBe(4000); expect(bomb.status).toBe('warning') })
  test('defuser is retry safe and does not change any streak state', () => { const bomb=createTierDBomb(10); const once=defuseTierDBomb(bomb,true); expect(once).toMatchObject({ status:'defused',defuserConsumed:true }); expect(defuseTierDBomb(once,true)).toEqual(once) })
  test('failure consequence remains configurable', () => { let bomb=createTierDBomb(10,{ ...require('../../src/game/tierDBombChallenge').tierDBombConfig,durationMs:1,failure:'forfeit_current_game' }); bomb=setTierDBombInteraction(bomb,true,0); expect(advanceTierDBomb(bomb,1)).toMatchObject({status:'failed',failure:'forfeit_current_game'}) })
})
