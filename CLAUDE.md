# CLAUDE.md — TriplePoker Project

> ไฟล์นี้คือกติกาบังคับสำหรับ Claude Code ทุก session
> อ้างอิง: TriplePoker_MasterPlan_v10_0 (Game Design Canon v1.0)

---

## 👤 เจ้าของโปรเจค

- **ลุงเยาะ** (Asst. Prof. Pongnathee Maneekul) — Founder & Chief Architect, The Sage Unicorn Studio Co., Ltd.
- Solo indie developer ทำทุก role
- เป้าหมาย: รายได้เสริม 10,000–30,000 THB/เดือน | **Launch: October 2026 (Android Only)**

---

## 🗣️ กติกาการสื่อสาร (บังคับทุกครั้ง)

1. **สื่อสารกับลุงเยาะเป็นภาษาไทยเสมอ** — ทุกคำอธิบาย ทุกคำถาม
2. **UI/labels/menus/popups/errors/system messages ในเกม: ภาษาอังกฤษทั้งหมด** (Global users)
3. **Code comments: ภาษาไทย**
4. **ก่อนสร้างหรือแก้ไขไฟล์ใดๆ ต้องถามยืนยันก่อนเสมอ ห้ามแก้โดยไม่ได้รับอนุญาต**
5. เมื่อเจอปัญหาหรือทางเลือก **เสนอ 2 แนวทางเท่านั้น:** 🅰️ MVP (ลดความซับซ้อน) และ 🅱️ Full System พร้อม recommendation ชัดเจน — ห้ามเสนอ 3 แนวทาง
6. **ถามประเด็นที่ยังไม่เคลียร์ให้ครบก่อนลงมือ ห้ามข้ามไปทำถ้ายังมีจุดคลุมเครือ**
7. QA รวม: เทสหาบั๊กทีเดียวหลัง feature ใหญ่ครบ ไม่ไล่แก้ minor bug ระหว่างทาง

---

## 🏗️ Architecture Rules (ห้ามละเมิด)

1. **แยกไฟล์ต่อ Tier** — ห้ามใช้ dynamic route `[tier]` เด็ดขาด
   - `app/game/initiate/` | `app/game/adept/` | `app/game/mastermind/` | `app/game/highNoble/`
   - Tier ใหม่ = copy จาก Tier ที่เสร็จแล้วมาแก้เฉพาะจุด (pattern หลักของโปรเจค)
2. **Rewrite แทน patch** เมื่อไฟล์สะสม patch มากจนเริ่ม corrupt
3. **Two Apps, One Database** — Main App + The Arena (Phase 3) ใช้ Supabase DB เดียวกัน
4. Config-driven ทั้งระบบผ่าน `gameConfig.ts` — ห้าม hardcode ค่า Ante/Pot/Fee ในไฟล์ Tier
5. Last Boss Tier อยู่ในแอปแยก (The Arena) — ห้ามเอา logic มาปนใน Main App
6. **Tier S+ Last Boss AI (locked 2026-08-08):** When implementing the playable Tier S+ table/runtime, it MUST use `server/src/arena/sovereign/lastBossAIEngine.ts`. Do not create a replacement Boss AI and do not wire the legacy `server/src/ai/lastBossAI.ts` into Tier S+. Pile 3 must remain Best 5 of 7. The Boss has fixed strength for every challenger: no retry-loss balancing, Full Moon, pity, player-specific, or hidden dynamic difficulty.

## 🛠️ Tech Stack

- **Client:** React Native + Expo (Android only MVP) + Zustand + Reanimated 4
- **Server:** Node.js + Fastify + Socket.IO + TypeScript
- **DB:** Supabase PostgreSQL (project ref: `aeinnlaxscikarzupprn`) + Upstash Redis (REST client)
- **Env:** WSL2 บน Windows | รัน Expo จาก `/client` ด้วย `npx expo start --clear` (LAN mode)

## 🌐 Dev Networking (updated 2026-07-13)

- WSL2 ใช้ mirrored networking mode (`.wslconfig`: `networkingMode=mirrored`) — WSL ได้ IP เดียวกับ Windows
- Server: Fastify port 3001, bind `0.0.0.0`
- Client ชี้ `http://192.168.1.106:3001` (LAN IP ของเครื่อง MSI, router อาจแจกใหม่ได้ — ถ้าต่อไม่ได้ให้เช็ค `ipconfig` ก่อน)
- Expo ใช้ LAN mode: `npx expo start --clear` (เลิกใช้ `--tunnel`)
- Firewall rules มีแล้ว: port 3001 (server), 8081 (Metro), Hyper-V VM firewall ตั้ง `DefaultInboundAction=Allow` แล้ว
- **ห้ามแนะนำ/ตั้ง `netsh portproxy` หรือ localtunnel อีก** — เป็นวิธีเก่าที่เลิกใช้แล้ว
- ทดสอบ: มือถือเปิด `http://192.168.1.106:3001/health` ต้องได้ response (คอมตัวเองอาจเข้า LAN IP ไม่ได้ — เป็น quirk ของ mirrored mode ให้ใช้ `localhost` แทน ไม่ใช่บั๊ก)

---

## 🐛 Known Bugs & Fixes (ห้ามทำผิดซ้ำ)

| # | กติกา |
|---|---|
| 1 | `userStore.userId` ต้อง sync จาก `authStore` ใน `_layout.tsx` — ไม่งั้น userId ว่างทั้งแอป |
| 2 | `game_join` ใช้ **pull model** เท่านั้น (กัน race condition `round_start` มาก่อน socket mount) |
| 3 | `public.users` ใช้ `user_id` เป็น PK ไม่ใช่ `id` — query ต้อง `.eq('user_id', session.user.id)` |
| 4 | หลัง `ALTER TABLE` ต้องรัน `NOTIFY pgrst, 'reload schema';` เสมอ |
| 5 | ห้ามเรียก `bestOf7` ผิด context — ใช้ `evaluateHand` ตรงๆ |
| 6 | `evaluateHand` รับเกิน 5 ใบทำ flush/straight เพี้ยน — ต้องใช้ `bestThreeFromN` เลือก best 3 ก่อน |
| 7 | AI arrangement foul filter ต้องใช้ `compareHands(h2, h3)` ตรงๆ ห้ามสร้าง fake hand |
| 8 | `autoSort.ts`: condition ต้องเป็น `n !== 11 && n !== 12` (รองรับมือ 12 ใบ) |
| 9 | `foulChecker.ts`: ใช้ `pile3.length < 3` (ไม่ใช่ `!== 5 && !== 3`) |
| 10 | **ห้ามใช้ Unicode `──` `→` `—` ใน .tsx files** — Babel parse ไม่ออก |
| 11 | แก้ `.env` แล้วต้อง restart Expo ถึงจะโหลดค่าใหม่ |
| 12 | `gfRevealedCards` เป็น `Record<string, string[]>` (per-player) |

---

## ⚖️ Economy & Legal Rules (ห้ามละเมิดเด็ดขาด)

1. **Crown Package (ซื้อด้วยเงินจริง) ห้ามไหลระหว่างผู้เล่นทุกกรณี** — ใช้ได้เฉพาะ The Crown Vault cosmetics เท่านั้น ห้ามใช้ Match Stake
2. Earned Crown (Token→Crown 1:5,000) เท่านั้นที่ใช้ Match Stake ได้
3. Fan tribute ให้ Human Boss: **Token เท่านั้น** ห้าม Crown ทุกกรณี
4. **ทุกโต๊ะทุก Tier ต้องมี Bot/AI อย่างน้อย 1 ตัวเสมอ** (ป้องกัน gambling classification — รวม VIP Private table)
5. Legal disclaimer ต้องคงไว้: "Crown และ Token ไม่สามารถแลกเปลี่ยนเป็นเงินได้"
6. Revenue Share Human Boss — **ยังไม่ตัดสินใจ** รอปรึกษาทนาย gaming law ห้าม implement

## 🎮 Game Design Canon (สรุปที่กระทบโค้ด)

- **Tier structure:** Initiate (2★) → Adept (3★) → Mastermind (4★) → High Noble (5★) → Last Boss (5★+⚡ #FFD76A, แอปแยก)
- **Blind Auction** ปลดล็อกที่ Mastermind ขึ้นไป
- **Triple Sweep Jackpot:** ชนะ 3 กอง = Pot ×2, Rake 5% (ยกเลิก Rake 10% แยกแล้ว — 2026-07-17), Penalty หาร 3 (ทุก Tier)
- **Auto Sort Fee** (มติลุงเยาะ 2026-07-25 — เขียนทับเลขตายตัวชุดเดิมทั้งหมด): คิดเป็น **% ของ Ante กอง 3** ไม่ใช่เลขตายตัว — C ฟรี | B 25% (=35) | A 33% (=165) | A+ 50% (=750) | Last Boss 50% (=1,500 แต่ Arena ห้าม Auto Sort อยู่แล้ว) · ยกเลิกระบบ free rounds ถาวร (`freeRoundsForNewUser` ลบทิ้งแล้ว) เสีย fee ทุกครั้งที่กด · เจตนา: ยิ่ง Tier สูงยิ่งแพง เพื่อกึ่งบังคับให้ผู้เล่นฝึกจัดไพ่เองก่อนขึ้น Arena · **อ่านค่าผ่าน `getAutoSortFee(tier)` ใน `gameConfig.ts` เสมอ ห้าม hardcode ตัวเลขอีก** (ต้นเหตุที่เคยขัดกัน 3 แหล่งคือมันถูก hardcode ไว้หลายที่) · ฝั่ง client รับผ่าน `autoSortFee` ใน payload `round_start` ไม่มีเลขของตัวเอง
- **Monarch spawn rate** (High Noble): Monarch 3% / Reaper 28% / Crag 25% / Cortex 25% / Cipher 19% — adaptive personality ล็อกตอน deal
- **Nine Sentinels** (Mastermind Conquest): Iron Wall / Chivalry / War Lord / Phantom / Dark Shark / Oracle / Jester / Phoenix / Black Magic — reuse pattern Four Gods, Jester สุ่ม weight 1-10 ใหม่ทุกเกม (pattern เดียวกับ Cipher)
- **Unlock Tier S:** ผ่าน A+ AND token > 1,000K (ไม่ผูก 9/9 Sentinels)
- **Arena:** Deck 53 ใบ (52+Joker), Joker อยู่ได้เฉพาะ Auction card1/card2/Pile3 hidden — ห้าม Auto Sort ทุกชนิด (Hardcore Rule)

---

## 📋 สถานะปัจจุบัน & งานคงค้าง

**เสร็จแล้ว:** Tier C, B, A (core flow), A+ 100% | Auth Flow | Lock-up Token | Room Registry (Redis) | DB 4+1 migrations (002_name_protection รันแล้ว) | Landing page | Sprint 8: `nameValidator.ts` (3-layer) + `POST /auth/register` | **The Nine Sentinels (Boss Selection + aiEngine 9 personalities + `conquered_sentinels`)** — select.tsx/story.tsx/index.tsx + gameLoop.ts wiring เสร็จ, รอ migration 005 + avatar assets ก่อนเทสจริง (ดู pending #3-4 ด้านล่าง) | **High Noble Multiplayer (3 Human จริง + Four Gods Boss)** — `highNobleMultiEngine.ts` ใหม่ทั้งไฟล์ (ไม่แตะ gameLoop.ts เดิม), `roomRegistry.ts` ผูก Boss seat เป็น Four Gods จริง, `gameSocket.ts`/`lobby.tsx`/`highNoble/index.tsx` ต่อครบ — เทสผ่านสคริปต์ 3-socket จริง (5 รอบเต็ม + disconnect mid-arrangement) ดู pending #10-12 ก่อนขึ้นจริง | **Monarch System จริง (Spec v1.3 — เขียนทับ mechanic เก่าที่เป็น placeholder ทั้งหมด)** — บอสลับ 3%+pity (max ของโต๊ะ, การันตีเกม 30) สุ่มตอนห้อง High Noble เต็ม, `monarchSpawn.ts` (spawn+pity DB), `monarchAI.ts` (ล็อคบุคลิกตาม hand strength ตอนแจกไพ่ Round 1 เท่านั้น เดเลเกตไป aiEngine.ts เดิม), Pot ×2 ระดับ match + `monarch_victories` + Dual-Track Performance Score (`psEngine.ts`: Career/`performance_score` ไม่รีเซ็ต + Season/`ps_season` รีเซ็ตตาม tournament) ใน `highNobleMultiEngine.ts`, `ascendantGate.ts` (เงื่อนไขเข้า Ascendant — ต่อจริงแล้วผ่าน `crownVaultService.ts`, ดู pending #14 เรื่อง migration ที่ยังไม่ได้รัน), client boss intro + Profile (Season PS เด่น/Career PS รอง/badge/hint) — เทสใหม่ 27 เทสผ่านหมด, migration 006 รันแล้ว — ระบบ `tier:'monarch'` queue เดิม (สลับบุคลิกทุก Round) ถูกลบทิ้งทั้งหมดแล้ว | **Token Flow Panel (Initiate / Adept / Mastermind — TokenFlowPanel_Spec_v1_1)** — ตารางมุมขวาบน 6 แถว (`G1/G2/G3/F&R/4P/Total`) บังคับกฎเหล็ก `Pot×3 + Fee&Rake + All Stack = 4 × Buy-in` เสมอ · `server/src/game/tokenFlow.ts` ใหม่ (pure function ล้วน + 28 unit tests) · `gameLoop.ts` wire 7 จุด **gate `tier === 'initiate'` ทุกจุด** Tier อื่นยังใช้ `calcDeltas()` เดิมไม่เปลี่ยนพฤติกรรม · ของใหม่ที่ไม่เคยมีมาก่อน: **Pot เป็น bucket จริง** (เดิมไม่มี Pot ในระบบเลย — net ทีเดียวตอน showdown) + Ante หักตอนต้นรอบ + `feeRake` สะสมทั้งเกม burn ตอนจบ + Triple Sweep ×2 ใน Tier C (ใช้สูตรเดียวกับ Mastermind/HighNoble: `bonus = ante กอง3 × จำนวนผู้แพ้` ดูดจาก stack ผู้แพ้จริง ไม่เสกเงิน) · **มติลุงเยาะ 2026-07-25:** AI ชนะเก็บเงินเข้ากระเป๋าตัวเอง (ไม่ burn ตาม Spec §3) · Foul รายคนคงพฤติกรรมเดิม · foul ครบทุกคน → Pot เข้า Fee&Rake · rake 5% ทุกกรณี · **ลบปุ่ม Rematch ของ Initiate แล้ว** (Spec §5.1 No Rematch — Buy-in ต้องล็อคตั้งแต่ต้นเกม) · เทสจริงบนมือถือผ่านครบแล้ว

> **ขยายไป Adept (Total 8,000)** — multiplayer 2H+2AI ใช้ `MultiMatchState` ผ่าน `startMultiRound`/`resolveMultiShowdown` (settleRound ตัวเดียวกับ Tier C) · ของใหม่: **Auto Sort Fee ระบบจริง** server-authoritative (`auto_sort_request` → `auto_sort_ack`, `autoSortUsed` เก็บฝั่ง server, client ต้องมี state `autoSortPaid` แยกจาก `sortDone` เพราะ `sortDone` reset ทุกครั้งที่ผู้เล่นสลับไพ่เอง — ถ้าใช้ตัวเดียวกันจะโดนเก็บเงินซ้ำในรอบเดียว) · เทสจริงผ่านแล้ว
>
> **ขยายไป Mastermind (Total 60,000 หลัง buy-in ขึ้นเป็น 15,000)** — Tier นี้ **ไม่ได้ใช้ showdown path เดียวกับ Tier C/B** แต่แตกไปที่ `resolveMastermindPile12()` แล้วจบที่ `finalizeGrandFinale()` ซึ่งเดิมหัก Ante **ตอนจบเกม** ไม่ใช่ต้นรอบ · แก้โดยเพิ่ม helper `usesTokenFlow(tier)` ใน `gameLoop.ts` แล้วแยก `finalizeGrandFinale` เป็น 2 เส้นทาง (**โค้ด calcDeltas เดิมของ High Noble/Last Boss ยังอยู่ครบไม่ถูกแตะ** เพราะใช้ฟังก์ชันเดียวกัน) · ⚠️ ห้ามหัก Ante ทั้งต้นรอบและตอนจบพร้อมกัน และ jackpot ต้องคิดที่เดียว (`settleMastermindRound` คิดให้แล้ว) · ทางเดินเงินใหม่ 2 ทางที่ Tier ล่างไม่มี: **Blind Auction bid → Fee & Rake** (มติลุงเยาะ: ไม่ burn ทันทีตาม `auctionBurn: 1.0` เพราะเงินหายกลางเกมจะทำให้กฎเหล็กพัง — ผลทางเศรษฐศาสตร์เท่ากัน ต่างแค่จังหวะ) และ **Grand Finale Call → Pot 3** · `state.extraPaid` เก็บยอดที่จ่ายระหว่างรอบไว้รายงานใน `displayDeltas` เท่านั้น **ห้ามนำไปหักซ้ำ** · เงินไม่พอ = ปุ่มถูก disable / ประมูลไม่ได้ไพ่ / ถูกบังคับ Fold ไม่มีทาง stack ติดลบ (มีเทสคุมทั้ง 3 เคส) · ไม่มีปุ่ม Rematch · เทสจริงผ่านแล้ว

> ⚠️ **UI ของ Initiate = ต้นแบบบังคับของทุก Tier ที่จะทำต่อ** (มติลุงเยาะ 2026-07-25): ปุ่ม `i` + Timer อยู่ซ้ายสุดผ่าน prop `leftSlot` ของ `GameTopBar` (opt-in — Tier ที่ไม่ส่งได้ layout เดิม) · `TokenFlowPanel` ชิดขอบบนขวา · ที่นั่ง Boss ใช้ `alignItems:'flex-end'` + `paddingRight: PANEL_WIDTH + PANEL_RIGHT` (import จาก TokenFlowPanel) ทำให้กรอบไพ่ชนขอบ Panel พอดีทุกขนาดจอ ห้ามใช้เลขตายตัว · ไม่มีปุ่ม Rematch · burn toast ตอนจบเกม
>
> ⚠️ **ย้าย `i` เข้า `leftSlot` แล้วต้องลบปุ่ม `i` เดิมที่ลอยอยู่มุมซ้าย (`position:'absolute', top:70`) ทิ้งด้วยเสมอ** — ไม่งั้นได้ปุ่ม 2 อันซ้อนกัน (เคยพลาดมาแล้วที่ Mastermind)
>
> ⚠️ **Grand Finale ใช้ปุ่ม `CALL` / `FOLD` จริงเท่านั้น** (มติลุงเยาะ 2026-07-25 — ใช้กับทั้ง Mastermind และ High Noble): เลิกใช้ gesture ทั้งหมดแล้ว (เดิม แตะไพ่ซ้ำ = Call · ปัดลง = Fold · มีกล่องคำอธิบาย 2 บรรทัด) · **แตะไพ่ = เลือกใบที่จะหงายเท่านั้น** ไม่ยิง action เอง กันกดพลาดเสียเงิน · ถ้าไม่เลือกใบไหน client จะไม่ส่ง `revealedCardKey` แล้ว server เลือกใบอ่อนสุดให้เอง (`pickRevealCard`) · `GFHandView` (VIP fan) เหลือ prop `onSelect` อย่างเดียว ไม่มี `onCall`/`onFold` แล้ว
>
> ✅ **Fan Hand View เสร็จแล้ว** (ของเดิมเข้าใจผิดว่ายังไม่ทำ) — `PlayerHandView.tsx` มี 2 โหมดตาม VIP status (Free: กรอบทองแถวเดียว / VIP: fan จริงแบบพัดไพ่จีน pivot-rotation) ต่อใช้จริงแล้วใน `highNoble/index.tsx`
>
> ✅ **Ascendant Tier + The Crown Vault (2026-07-30)** — เชื่อม Main App กับ The Arena: entry gate จริง (`ascendantGate.ts`: token≥600k + Monarch Slayer + เคยปลด HighNoble + ครั้งเดียวต่อบัญชี, ไม่มี Account Age Gate — 180 วันเป็นเงื่อนไขคนละเส้นทาง "เส้นทางสำรอง" ผ่าน `progressionGate.arena`), `crownVaultService.ts` ใหม่คุม Buy Ascendant Pass (instant pass ถ้า token≥1M อยู่แล้ว)/Buy Arena Pass (ด่านสุดท้ายบังคับทั้ง 2 เส้นทาง)/Token→Crown Exchange, `routes/crownVault.ts` แยกจาก `shop.ts` เดิมที่ยังไม่ได้ register, atomic RPC ใหม่ 4 ตัวกัน race condition (`deduct_user_tokens`/`credit_user_crown`/`deduct_user_crown`/`exchange_token_to_crown`) — เทสใหม่ 25 เทสผ่านหมด, migration 010 ยังไม่ได้รัน (ดู pending #14)

**Pending (เรียงตาม priority):**

> ✅ **ปิดแล้ว 2026-08-14:** Profile streakDays (ต่อ `streak_count` จริงอยู่แล้ว, ลบ `profile_arena_stone.tsx` prototype เก่าทิ้ง) · Auth Guard display name pattern (client+DB trigger ตรงกันที่ 4 hex แล้ว) · Migration 005 Nine Sentinels (ลุงรันแล้ว) · Nine Sentinels avatar crop (`_avatar.png` มีอยู่แล้วครบ 9 ตัว ต่อเข้า `mastermind/index.tsx`'s `BOSS_AVATAR` map แล้ว — `select.tsx`/`story.tsx` ยังคงใช้ portrait เต็มตามเดิมถูกต้องแล้ว) · **Monarch token persistence gap** (เดิมข้อ #8 — เขียนไว้ 2026-07-11 ว่าแก้เฉพาะ High Noble บั๊กเดียวกันน่าจะมีใน single-player/Adept ด้วย) **ล้าสมัย เอกสารเขียนผิด** — จริงๆ ปิดไปแล้วตั้งแต่ 2026-07-12 (commit `fdc2b5e` "Add Buy-in Escrow model") ด้วย `settleEscrow()` ตัวเดียวใช้ร่วมทุก Tier (single-player/Adept/HighNoble) ไม่ได้แยกเฉพาะ HighNoble เหมือนที่จดไว้ — งาน Central Economy Ledger (Round 1-2/4, หลังจากนั้นอีกเดือน) มาเจอ+แก้บั๊กคนละตัวที่ซ้อนทับอยู่บนนั้นอีกที (ชนะแล้วได้แค่ net ไม่ได้ finalStack เต็ม) ผ่าน `economyService.settleMatchResult()`'s `burnOverride` เสร็จเรียบร้อยแล้วเช่นกัน ยืนยันด้วยเทส `*LedgerSettlement*.test.ts` 50/50 ผ่าน

1. Auction bid style ตัวเลข (willBid %/level) ของ 9 Sentinels ใน `gameLoop.ts` เป็นการตีความจากคำอธิบายเชิงคุณภาพใน MasterPlan (canon ให้แค่คำบรรยาย ไม่ใช่ตัวเลข) — ควรปรับจูนหลัง playtest จริง
2. **Auto Sort Fee ฝั่ง High Noble ยังไม่หักเงินจริง** — ระบบทำเสร็จแล้วสำหรับ Adept (35) และ Mastermind (165): server-authoritative เต็มตัว (`requestAutoSort` ใน `gameLoop.ts` รองรับทั้ง `multiMatchStates` และ `matchStates`, client ได้แค่ "ขอ" ผ่าน `auto_sort_request` แล้วรอ `auto_sort_ack` ถึงจะจัดไพ่ให้) · ⚠️ **High Noble ปุ่มแสดง `750` ถูกต้องแล้วแต่เป็น hardcode และยังไม่หักเงิน** เพราะ Tier นั้นยังไม่ได้ wire Token Flow — ตอนทำ Tier A+ ต้องเปลี่ยนไปรับ `autoSortFee` จาก payload `round_start` เหมือน 2 Tier ก่อนหน้า และเอา `costBadge="750"` ที่ hardcode ไว้ออก
3. Boss Card Counting AI Enhancement (Pile2 Winner Signal) สำหรับ Crag+Cipher
4. **QA รวม:** สถานะเทสจริง ณ 2026-07-25 (หลังงาน Token Flow ครบ 3 Tier) = **232 passed / 7 failed (239 total, 16 suites)** — รายการ 6 suites เดิมที่เคยจดไว้ล้าสมัยแล้ว (`foulChecker`/`itemPhaseController`/`minionAI` กลับมาผ่านแล้ว ส่วน `pileResolution`/`aiFillSystem`/`blindAuction` ไม่มีไฟล์ในโปรเจคแล้ว) · ที่พังจริงตอนนี้เหลือ 2 suites: **`psEngine.test.ts` (6 เทส)** + **`adeptAFK.test.ts` Case 5 (1 เทส — `settleAndEndMultiMatch` คืน null)** ทั้งคู่พังมาก่อนงาน Token Flow Panel (พิสูจน์แล้วด้วยการ revert `gameLoop.ts` กลับ HEAD แล้วรันเทียบ) ต้องไล่แก้รวดเดียวตอน QA รวม + เพิ่ม QA flow ของ Mastermind Conquest (select→story→5 รอบ→conquest overlay→9/9 message) และ Monarch System (spawn/pity→personality lock→Pot×2 settlement→PS dual-track) เข้าไปในรอบ QA รวมนี้ด้วย · **อัปเดต 2026-07-30:** เทสรวมทั้งโปรเจค (รวมชุด Ascendant/Crown Vault ใหม่) ผ่านครบ 298/298 (20 suites) แล้ว — 2 suites ที่เคยพังข้างต้นกลับมาผ่านแล้วเช่นกัน แต่ยังไม่ได้ตรวจว่าแก้จากงานไหน เก็บไว้ยืนยันอีกทีตอน QA รวมจริง
5. **ก่อน push ขึ้น GitHub ครั้งแรก:** ต้องล้าง GitHub token ที่หลุดใน git history ของ `server/jest.config.js` (commit `ec11cc4`) ก่อน — ใช้ `git filter-repo` หรือ BFG rewrite history (destructive, ต้องขอยืนยันลุงก่อนรันจริง)
6. **High Noble Multiplayer — ยังไม่เทสผ่าน UI จริง:** สร้าง/เทสผ่านสคริปต์ socket.io-client ล้วน (bypass เบราว์เซอร์) เพราะ Dev Login ใช้ไม่ได้ (`test1@triplepoker.dev`/`password` ขึ้น "Invalid login credentials" จริงจาก Supabase) — ต้องหา dev account ที่ใช้ได้ หรือปิด email confirmation ชั่วคราวใน Supabase dashboard ก่อนเทส 3 เบราว์เซอร์จริง
7. **High Noble Multiplayer Grand Finale reconnect:** `resendHNRoundStartToPlayer` (ใช้ตอน client เปิด socket ใหม่หลัง matchmaking) รองรับแค่ phase `'arrangement'` เหมือนต้นแบบ Adept เดิม — ถ้า client หลุดแล้วต่อกลับระหว่าง auction/discard/grand_finale จะไม่ได้ state คืน (ต้องรอ disconnect handler แทนที่ด้วย AI แทน ไม่ true-reconnect)

8. **`server/tsconfig.json` พัง — `tsc` ใช้ตรวจ type ไม่ได้ทั้งโปรเจค:** `include` มี `tests/**/*` แต่ `rootDir` ตั้งเป็น `./src` → ทุกไฟล์ใน `tests/` โยน `TS6059` ออกมาหมดทุกครั้งที่รัน `tsc --noEmit` (น่าจะเป็นต้นเหตุที่ pending #4 เคยจดว่า "ส่วนใหญ่เป็น TS type error") โค้ดใน `src` เองสะอาด 0 error · แก้ได้ด้วยการเพิ่ม `tsconfig.build.json` แยกสำหรับ build หรือถอด `rootDir` ออก — เก็บไว้ทำตอน QA รวมตามกติกา "ไม่ไล่แก้ minor ระหว่างทาง"
9. **Ascendant Tier + Crown Vault — รอรัน migration + เทสจริง:** Backend (`ascendantGate.ts`/`crownVaultService.ts`/`routes/crownVault.ts`) และ Client MVP tab (`ShopScreen.tsx`'s "CROWN VAULT") เสร็จแล้ว เทสผ่าน 25 unit tests (ดูรายละเอียดที่ `crownVaultService.test.ts`/`ascendantGate.test.ts`) — ⚠️ ต้องรัน `supabase/migrations/010_ascendant_crown_vault.sql` บน Supabase dashboard เองก่อน (มี `NOTIFY pgrst, 'reload schema';` ให้แล้ว) ถึงจะเทส endpoint จริงได้ · ยังไม่ได้เทส UI ผ่านเบราว์เซอร์/มือถือจริงเลย (แซนด์บ็อกซ์รัน `nodemon`/WSL2 dev server ไม่ได้)
10. **Grandmaster (Tier S) — Audit 2026-08-07, Round 1+2 เสร็จ 2026-08-07:** เนื้อเกมจริงต่อครบวงจรแล้ว (จัดไพ่ → ตัดสินผู้ชนะ → หักเงินจริง) รายละเอียด:
    - **Round 1 (commit `aaa7b2e`):** gameplay projection จริงต่อ viewer ผ่าน `arenaProjection.ts` ใหม่ + personalized emit ใน `arenaSocket.ts` + บอท/AI ตอบสนองทันทีผ่าน `driveBots()`
    - **Round 2:** ไพ่ 11 ใบแบ่ง 3/3/5 (pile3 ยืดหยุ่น 3-6 ก่อน Discard) แบบเดียวกับ Tier ล่างทุกประการ (`server/src/arena/arrangement/arenaArrangement.ts` ใหม่ — พอร์ตจาก `foulChecker.ts`/`aiEngine.ts` ให้ใช้ `evaluateArenaHand`/`ArenaCard`) · `arenaMatchEngine.ts`: action `ARRANGE_1`/`FINAL_ARRANGE`/`FINAL_LOCK` รับ `{pile1,pile2,pile3}` จริงแทน `arrangementHash`, validate ทุกครั้งว่าตรงกับไพ่ที่ถืออยู่จริง (`heldCardIds`, canonical id = `arenaCardKey()` เดียวกับที่ client เห็น ไม่ใช่ `card.id` ดิบ — client reverse-map กลับไม่ได้), `RESOLVE_PILE_1/2/3` ตัดสินผู้ชนะจริงผ่าน `evaluateArenaHand` (solo-GF-survivor ชนะไม่ต้องเปิดไพ่ตาม spec §7.6) · **Settlement engine ต่อเข้า runtime จริงแล้ว** — `ArenaMatchEngine` มี `ArenaSettlementEngine` ฝังใน ยิง command จริงทุกจุด (Ante ตอน Deal, Auction/Call ตอนเกิดขึ้น, Boss Fee ครั้งเดียวตอนเริ่ม Match, Pile Payout ตอน resolve, Sweep Jackpot, End Match) → `arenaSocket.ts` drain แล้ว persist ผ่าน `ArenaSettlementPersistence` จริง, `BUY_IN_RESERVED` เช็คยอด `ArenaCrestLedger` จริงก่อนเข้าห้อง (ไม่หักเงินล่วงหน้า หักตามจริงที่เกิดขึ้น) · Client: `PlayerHandView.tsx` (shared component เดิมของ Tier ล่าง) ใช้จัดไพ่ tap-to-swap ใน `GrandmasterTableView.tsx` ช่วง ARRANGE_1/FINAL_ARRANGE/FINAL_LOCK + Discard sheet ใหม่
    - **Trade-off ที่ตั้งใจ (บันทึกไว้กันงงทีหลัง):** `bestArenaArrangement` (ใช้ทั้งเป็น AI arrangement และ timeout default) เปลี่ยนจาก brute force เต็มรูปแบบ (แบบ `aiEngine.ts` เดิม) มาเป็น greedy **ไม่เช็ค foul** เพราะ Joker wild ทำให้ `evaluateArenaHand` แพงกว่า `evaluateHand` มาก brute force เดิมช้าเกิน (~500ms/ครั้งตอน 12 ใบ) จะบล็อก event loop ทุก tick — บอทอาจ foul เองได้บ้าง (แค่เสียกองนั้น ไม่ crash)
    - **ยังไม่ทำ (milestone ถัดไป):** ~~UI "เปิดไพ่ฝ่ายตรงข้ามตอน showdown"~~ ทำแล้วใน Round 5 ด้านล่าง · Sovereign (S+) ยังไม่มีหน้าจอโต๊ะเล่นจริง มีแค่ lobby (qualify/check-in/standby)
    - **⏸️ มติลุงเยาะ 2026-08-14: พัก Tier S+ (Sovereign/Last Boss) ไว้ก่อน ไม่ทำใน MVP แล้ว** — เวลาที่เหลือก่อน Launch ตุลาคม 2026 ค่อนข้างจำกัด ตัดสินใจย้ายไปเก็บงานค้างอื่นให้เสร็จก่อนแทน (ตรงกับที่ "Last Boss UI" อยู่ใน **Deferred to v1.1** อยู่แล้วด้านล่าง — รอบนี้แค่ยืนยันมติชัดเจนอีกครั้งพร้อมเหตุผล ไม่ใช่การตัดสินใจใหม่) Tier S (Grandmaster) เองยังคง in-scope MVP ตามเดิมไม่กระทบ (เนื้อเกมจริงต่อครบวงจรแล้ว) — เฉพาะ Tier S+ เท่านั้นที่เลื่อน
    - เทส: `server/tests/arena/arenaArrangement.test.ts` ใหม่ + `arenaMatchEngine.test.ts` เพิ่ม settlement conservation test (ยืนยัน Crest รวมทั้งโต๊ะไม่หาย/ไม่เกิดเพิ่มตลอด Match จริงหลายรอบ) — ลด stress-test seed จาก 1,000 เหลือ 6-20 เพราะ arrangement คำนวณจริงแล้วช้ากว่า string ฟรีเดิมมาก (matchEngine test suite ทั้งไฟล์ ~50-90s)
    - **Round 3 (Boss AI Personality):** บอทเดิมทุกตัว (Four Gods/Monarch/Soren/Arena Minion) วิ่งผ่าน policy ว่างเหมือนกันหมด (บิด 0 เสมอ, Fold GF เสมอ) — ต่อบุคลิกจริงเฉพาะที่นั่ง Boss (role==='BOSS' เท่านั้น, ARENA_MINION ยังโง่เหมือนเดิมตามตั้งใจ) ผ่านโมดูลใหม่ `server/src/arena/ai/arenaBotPersonality.ts` (REAPER/CRAG/CORTEX/CIPHER — พอร์ตตัวเลข willBid%/bidLevel/callProb ตรงจาก `aiEngine.ts`/`gameLoop.ts:876-940,1646-1668` เป๊ะ + `lockArenaBossPersonality()` พอร์ตกลไก "ล็อกบุคลิกตามความแข็งไพ่ตอนแจกเกม 1" จาก `monarchEngine.ts:307-319,374-388` ให้ Monarch, อ่าน threshold จาก `gameConfig.monarchConfig.handStrengthQuartile` ตัวเดียวกับของเดิมไม่ duplicate เลข) และ `arenaSorenPersonality.ts` ใหม่ทั้งหมด (ไม่มี precedent — ฐาน cipher-like + ปรับ bias ตามสถิติ Call/Fold ของ Human สะสมข้ามเกมในแมตช์เดียวกัน + bluff-flip ~20%) ประกอบเป็น policy จริงผ่าน `resolveArenaBotPolicy.ts` ใหม่ ที่ `arenaSocket.ts`'s `driveBots()` เรียกแทนการส่ง `{}` ว่างเหมือนเดิม
    - **Scope cut ที่ตั้งใจ:** จัดไพ่ (arrangement) ยังใช้ `bestArenaArrangement` แบบ personality-neutral เดิมสำหรับทุกบอทรวม Boss เพราะ greedy solver ไม่มี weighted-sum step ให้ผูก personality ได้ธรรมชาติ + เป็นข้อมูลส่วนตัวที่ผู้เล่นไม่เห็นระหว่างเล่นอยู่แล้ว — บุคลิกแสดงออกผ่าน Auction/Joker/GF เท่านั้น (จุดที่ผู้เล่นสัมผัสได้จริง)
    - **บั๊กที่เจอและแก้ระหว่างทาง:** `ArenaMatchEngine.pileHandFor()` (ใหม่ ให้บอทประเมิน winrate ของกองเป้าหมาย) เรียก `evaluateArenaHand` ตรงๆ ตอนแรก แต่ก่อน Discard pile3 อาจมี 6 ใบ + community 2 = 8 ใบ เกินขีดจำกัด evaluator (5-7) → throw ทุกครั้งที่ auction winner ต้องตัดสินใจ Joker/GF ก่อน Discard, ถูก `driveBots()`'s try/catch กลืน error เงียบๆ ทำให้บอทหยุดตอบสนองกลางทาง แมตช์ AI ล้วนต้องรอ phase timeout เต็มแทน (ทดสอบจริงพบเทสช้าลงจาก ~44s เป็น ~535s ถึงจับได้) — แก้โดย export `evaluatePileBest()` จาก `arenaArrangement.ts` (มี fallback ลอง drop ทีละใบอยู่แล้ว) มาใช้แทน
    - **Round 4 (Boss Card Counting — ปิด pending #3 เดิมสำหรับ Arena):** ตอบคำถามลุงเยาะเรื่อง Monarch/Soren "อ่านไพ่คู่ต่อสู้กอง 3 ยังไง" แล้วพบว่า Round 3 ยังไม่มี card counting เลย ใช้แค่ rankIndex ของไพ่ตัวเองแปลงเป็น winrate เท่านั้น — เพิ่ม `ArenaMatchEngine.estimateOpponentSafeRate(actorId, pile)` ใหม่ (พอร์ตแนวคิด worst-case-จาก-unseen-pool ของ `gameLoop.ts:1574-1635`'s `estimateBossWinrate` เดิม แต่ใช้ greedy หยิบไพ่ danger สูงสุดแทนลองทุก combination กันคำนวณหนัก) มี known lower bound เพิ่มจากของเดิม: ผู้ชนะกองก่อนหน้าที่ **เปิดเผยไพ่จริง** (เทียบไพ่แพ้ชนะจริง ไม่ใช่ชนะเพราะคู่แข่ง Fold จนเหลือคนเดียว — เคารพกฎ "ไพ่ผู้แพ้ไม่เปิดเผย" เหมือนผู้เล่นคนอื่น ใช้ `pile2Revealed`/`pile3Revealed` คุม) ไพ่กองนั้นต้องแข็ง ≤ กองถัดไปของเขาเองเสมอ (กฎ pile1≤pile2≤pile3) ใช้เป็นสัญญาณรู้แน่ได้เลยไม่ต้องเดา · ใช้กับทั้งกอง 2 (lower bound จากกอง 1) และกอง 3 (lower bound จากกอง 2) ตามที่ลุงเยาะยืนยัน · สูตร bias ของแต่ละบุคลิก (Reaper +0.20 ฯลฯ) **ใช้ต่อเดิมทั้งหมด ไม่แก้** แค่เปลี่ยนแหล่งที่มาของ winrate ที่ป้อนเข้าสูตร (`resolveArenaBotPolicy.ts`'s `winrateForGf`) จากดูไพ่ตัวเองอย่างเดียว มาเป็น `estimateOpponentSafeRate` แทน · Joker declare ยังใช้ไพ่ตัวเองอย่างเดียวเหมือนเดิม (ตัดสินใจก่อน pile1/pile2 resolve จึงไม่มี opponent signal ให้ใช้) · เทสใหม่ `arenaCardCounting.test.ts`
    - **Round 5 (2026-08-08, Pile-Winner Reveal Overlay — ปิด "ยังไม่ทำ" เรื่อง showdown reveal ด้านบน):** ลุงเยาะเทสจริงบนมือถือแล้วขอ UI โชว์ไพ่ผู้ชนะแต่ละกองลอยขึ้นมา ให้เหมือน pattern ของ VIP Plus's `roundResultOverlay` — สำรวจโค้ดพบว่า VIP Plus ทำ **หยุดเกมจริง ~6 วิ** ระหว่างโชว์ (`gameConfig.ts`'s `vipPlus5.resultDisplayMs` ผ่าน `extraDelayMs` ใน `vipPlusMatchEngine.ts`) ไม่ใช่แค่ overlay ลอยๆ — ลุงเยาะยืนยันให้ทำ Full System (หยุดเกมจริงเหมือนกัน) แทน MVP client-only · เพิ่ม phase ใหม่ 3 ตัวเข้า state machine หลัก **`REVEAL_PILE_1`/`REVEAL_PILE_2`/`REVEAL_PILE_3`** (มี `arenaPhaseTimeoutMs` จริง ~4s ต่อกอง) แทรกระหว่าง `RESOLVE_PILE_X` (เดิมเป็น `AUTOMATIC_PHASES` ที่ cascade ข้ามไปเลยในทิกเดียว ไม่มีใครเคยเห็น phase นี้จริง) กับ phase ถัดไปเดิม (`GF_PILE_2`/`GF_PILE_3_ROUND_1`/`CHECK_SWEEP_JACKPOT`) · ทางออกเดียวของ 3 phase ใหม่คือรอ deadline หมดแล้ว `progressAfterDecision()` เป็นคนส่งต่อเอง ไม่มี action ให้กด (`pendingActors()` คืน `[]` เองอยู่แล้วไม่ต้องแก้) · `ArenaMatchEngine.capturePileReveal()` ใหม่ เก็บผู้ชนะ/hand rank/ไพ่ที่ใช้จริงไว้โชว์ ผ่าน `pileHandFor()` เดิม (**gotcha**: `ArenaHandResult.selectedCardIds` เป็น `card.id` ดิบ ไม่ใช่ `arenaCardKey()` ที่ `dealCardsById` ใช้เป็น key ต้องแปลงผ่าน local lookup ก่อนส่งออก) · เคารพ Fog of War เดิมทุกกรณี — ถ้าผู้ชนะกอง 2/3 มาจากคู่แข่งทุกคน Fold หมดไม่ได้เทียบไพ่จริง (`pile2Revealed`/`pile3Revealed` เป็น false) จะไม่โชว์ไพ่/hand name แม้เป็นของผู้ชนะเอง (เห็นแค่ชื่อคนชนะเฉยๆ) · `arenaProjection.ts` ส่งออกเป็น field `reveal` ใหม่ (non-null เฉพาะช่วง 3 phase นี้ ตาม convention เดิม "1 field ต่อ 1 overlay ที่กำลัง active") · Client `ArenaOverlays.tsx`: overlay เต็มจอใหม่ ใช้ `CARD_IMG`/`CARD_BACK_IMG` เดิม ไพ่ 5 ใบที่ใช้จริงกระพริบด้วยเทคนิคเดียวกับ VIP Plus's `BlinkingCard` (Reanimated `withRepeat`/`withSequence`) ทั้ง block ลอยขึ้น+จางเข้ามาตอน mount ด้วยเทคนิคเดียวกับ VIP Plus's `FloatingDeltaText` (`withTiming` บน `translateY`) — **ไม่ต้องมี client-side dismiss timer เลย** เพราะ server phase เองเป็นตัวจับเวลาอยู่แล้ว overlay หายเองพอ `reveal` กลับเป็น `null` ตอน server เปลี่ยน phase (ง่ายกว่า/robust กว่า VIP Plus's client `setTimeout` safety-net เดิมด้วยซ้ำ) · **Scope cut ที่ตั้งใจ:** payout โชว์เป็นข้อความ "+N Crest" เฉยๆ ไม่ได้พอร์ต `FlyingCoins`/`FloatingDeltaText`'s seat-position-relative flight VFX มาด้วย เพราะ Arena ยังไม่มี seat-target coordinate map แบบ rotation-aware (ที่นั่งหมุนตาม `local.seat` จริงจาก fix รอบก่อน) — ทำทีหลังได้ถ้าต้องการ · **Test-infra fix ที่จำเป็นคู่กัน:** driver แบบ `while (pending.length) submit else tick(now++)` ที่ใช้ซ้ำใน `arenaMatchEngine.test.ts`/`arenaCardCounting.test.ts` เดิมบวก `now` ทีละ 1ms ตอนไม่มี actor รอ (เดิมโอเคเพราะทุก phase ไม่มี actor รอเป็น automatic จบใน 1 รอบ) พอมี `REVEAL_PILE_X` ที่มี deadline จริง ~4000ms ต้องแก้ให้กระโดด `now` ไปที่ `deadlineAt` ตรงๆ กัน loop วนหลักพันรอบ (ไม่งั้นเทสช้าลงมาก) · เทสใหม่/ขยาย: `arenaMatchEngine.test.ts` (REVEAL_PILE_1 หยุดจริง+auto-advance, ขยาย solo-GF-survivor test ให้เช็ค Fog of War ของ `pileReveal`), `arenaProjection.test.ts` (`reveal` null นอก phase, ตรงกันทุก viewer) · Full suite 595/597 ผ่าน (2 fail เป็นไฟล์ VIP Plus 5-player ที่ยังไม่ track/ไม่เกี่ยวกับงานนี้ ต้นเหตุ `.env`'s `VIP_PLUS_5P_ENABLED=true` เดิม)
    - **Round 6 (2026-08-08, Nine Sentinels แทน Minion ทั่วไป + ยืดเวลารอคนที่ 3):** ลุงเยาะถามว่าตอนนี้เปิดโต๊ะได้กี่แบบ แล้วบอกไม่อยากให้เจอ Minion บ่อย อยากได้โต๊ะ 3H+Boss AI เป็นหลัก — สำรวจ `arenaMatchmaking.ts` แล้วพบว่าดีไซน์เดิมตรงกับที่ต้องการอยู่แล้ว: โต๊ะได้ Minion เติมที่นั่งเฉพาะกรณีเดียวคือติดอยู่ 2 Human ครบ `fastBossRollWindowSeconds` โดยไม่มีคนที่ 3 มาทัน (มีคนที่ 3 เข้ามาเมื่อไหร่ ไม่ว่าเร็วหรือช้า การันตี Boss AI เดี่ยวเสมอไม่มี Minion) — ปรับ **`fastBossRollWindowSeconds` 60→180 วิ** (รอคนที่ 3 นานขึ้นก่อนถอยไปทาง 2H) และ **`bossEncounterRate` 0.6→0.2** (Four Gods กลายเป็น 80% แทน Monarch/Soren 20%, เฉพาะฝั่ง 3-human path) · ของใหม่ที่ทำจริง: **ตัวเติมที่นั่ง FILL ตอน 2H timeout เปลี่ยนจาก `'ARENA_MINION'` เฉยๆ เป็นสุ่ม 1 ใน 9 Nine Sentinels จริง** (`arenaMatchmaking.ts`'s `SENTINELS` array) พร้อม**บุคลิกจริงเต็มรูปแบบเหมือน Four Gods** (ลุงเยาะยืนยัน Full ไม่ใช่แค่เปลี่ยนชื่อ) — พอร์ตตาราง willBid%/level (Auction) และ threshold (GF Call/Fold) ตรงจาก `gameLoop.ts:896-934`/`gameLoop.ts:1669-1691` เป๊ะ (canon เดียวกับที่ Mastermind Conquest ใช้ "ห้ามแก้ค่า") เข้า `arenaBotPersonality.ts` (ขยาย `ArenaPersonality` type จากเดิม 4 ตัวเป็น 13 ตัว) · Joker declare ไม่มี precedent เหมือน Four Gods เดิม เลยจัดกลุ่มตามบุคลิก: บุก/ดุ (war_lord, dark_shark, phoenix) เอียง ANTE_X2 เหมือน reaper, ระวังตัว (iron_wall, black_magic) WILD เสมอเหมือน crag, EV นิ่งๆ (oracle, chivalry) ใช้เกณฑ์ cortex, ผันผวน (phantom, jester) สุ่มเหมือน cipher · **Scope cut ที่ตั้งใจ:** black_magic ต้นฉบับปรับ bid level ตาม token balance หลักพัน แต่ Arena ใช้ Crest ทั้งแมตช์แค่ ~228 สเกลไม่เทียบกันได้ เลยลดรูปเป็น level กลางคงที่แทน · arrangement (จัดไพ่) ยังคง personality-neutral เหมือนเดิมสำหรับทุกบอทรวม Sentinel (เหตุผลเดียวกับที่ Round 3 ตัด scope Four Gods — greedy solver ไม่มี weighted-sum hook + เป็นข้อมูลส่วนตัว) · `resolveArenaBotPolicy.ts`'s gate เดิมเช็คแค่ `role==='BOSS'` ขยายเป็นยอมรับ `role==='FILL'` ด้วยถ้า `aiId` อยู่ใน `SENTINEL_PERSONALITY` map ใหม่ (ที่นั่ง FILL แบบ `ARENA_MINION` ทั่วไปจาก `createHumanBossComposition` ที่ยังไม่ได้ใช้งานจริงวันนี้ ยังคง policy ว่างเหมือนเดิมไม่กระทบ) · **Client ไม่ต้องแก้อะไรเลย** — `displayName` derivation เดิม (`aiId.split('_').join(' ')`) แปลงชื่อ Sentinel ถูกต้องอยู่แล้วโดยไม่ต้อง special-case และไม่มีไฟล์ client ไหนอ้างอิง `aiId`/`ARENA_MINION` สำหรับ Arena เลย · เทสใหม่: `resolveArenaBotPolicy.test.ts` ใหม่ทั้งไฟล์ (ไม่เคยมี unit test แยกของฟังก์ชันนี้มาก่อน เดิมเทสผ่าน full-match simulation อ้อมๆ เท่านั้น) + ขยาย `arenaBotPersonality.test.ts`/`arenaMatchmaking.test.ts` · Full suite 612/614 ผ่าน (2 fail pre-existing: `vipPlusAccess.test.ts` เดิม + `hnGracePeriod.test.ts` flaky ยืนยันผ่าน 14/14 ตอนรันเดี่ยว ไม่เกี่ยวกับงานนี้)
    - **แก้บั๊ก (2026-08-08): Avatar ผู้เล่นจริงไม่โชว์เลย** — ลุงเยาะถามว่าโชว์รูป Avatar หน้าชื่อผู้เล่นหรือยัง เจอว่าหลุดหาย 3 ชั้นต่อกัน: `arenaSocket.ts`'s `authenticate()` ดึง `avatar_url` มาถูกแล้วแต่ `identities` map เก็บแค่ `displayName` ทิ้ง avatar ไป → `arenaProjection.ts` ไม่เคยพยายามใช้เลยแทนด้วยสัญลักษณ์ตายตัว (`♛`/`♠`) → client render เป็น `<Text>` ไม่ใช่รูป — แก้ครบ 3 ชั้น เพิ่ม field `controller: 'HUMAN'|'AI'` ใหม่ให้ client แยกได้ว่า seat ไหนควร resolve avatar จริง (`AvatarDisplay` จาก `client/src/components/profile/AvatarPicker.tsx` — component เดียวกับที่ VIP Plus ใช้อยู่แล้ว) ส่วน AI ยังใช้สัญลักษณ์เดิมไม่เปลี่ยน · กรอบเป็น decorative เดียวกันทุกคน (`frameKey:'default'`) ไม่ใช่ personalized เพราะระบบ Frame เลือกเองยังไม่เปิดใช้งานจริงทั้งระบบ (comment ใน `AvatarPicker.tsx` เองยืนยัน) ไม่ใช่แค่ Arena ยังไม่ได้ต่อ
    - **Round 7 (2026-08-08, Monarch & Soren Boss Intro):** ลุงเยาะขอหน้า Intro แนะนำ Monarch/Soren ตอนเจอที่โต๊ะ (Four Gods ไม่ต้องมีเพราะผู้เล่นเจอมาแล้วตั้งแต่ High Noble) — พบว่าเป็นงาน "อัปเกรดเนื้อหา" ของสิ่งที่มีอยู่แล้วเกือบทั้งหมด ไม่ใช่ปลั๊กใหม่: `arenaProjection.ts` คำนวณ `bossPresentation` (`gameNumber===1 && phase==='ARRANGE_1'` + boss เป็น Monarch/Soren) อยู่แล้วตั้งแต่ Round 1 แต่ `ArenaOverlays.tsx` render เป็นแค่ banner ลอยเล็กๆ กว้าง 300px 3 บรรทัด ไม่สมกับที่ขอเลย — เจอ pattern ที่แรงกว่ามาก 2 ที่ในโปรเจคให้พอร์ต: `client/app/game/monarch/index.tsx`'s `showBossIntro` (portrait เรืองแสงเต็มจอ + ปุ่มกดเข้าเกม บล็อกจนกว่าจะกด) และ `mastermind/story.tsx`'s portrait→name→motto fade sequence · เจอ lore จริงใน `docs/TriplePoker_Universe_Story_and_Cross_App_Integration_Spec.md` §2.1-2.2: Monarch = **THE WATCHER** (quote มีอยู่แล้วจริงใน `monarch/index.tsx`: "Power is easy to claim. Restraint is harder to prove.") Soren Veyl = **THE FIRST EXILE** (บุคลิก calm/dry humor/irreverent toward titles — quote เป็น draft ใหม่ที่ยังไม่มี canon จริงเหมือน Monarch รอลุงเยาะแก้ได้) · เจอบั๊กข้อมูลผิดไปด้วย: banner เดิมเขียน Monarch เป็น `'MONARCH | FOUR GODS'` ทั้งที่ตาม canon Monarch เป็นคนละ archetype กับ Four Gods เลย แก้พร้อมกัน · เปลี่ยน shape `bossPresentation` จาก `{title,subtitle,dialogue}` เดี่ยว เป็น `{title,subtitle,atmosphere,quote}` สองจังหวะ · Client component ใหม่ `BossIntroOverlay` ใน `ArenaOverlays.tsx` ใช้ Reanimated (`withDelay`/`withTiming`) แทน classic `Animated` ของ `monarch/index.tsx` เดิม ให้ตรง convention ไฟล์นี้ (เดียวกับ `PileRevealOverlay` ของ Round 5) · ลุงเยาะเพิ่มรูป `boss_soren.png`/`boss_soren_avatar.png` ให้เองก่อน (ของเดิมไม่มีรูป Soren เลยสักไฟล์ในโปรเจค) ตาม pattern เดิมของ `boss_Monarch.png` · Full suite 615/617 ผ่าน (2 fail pre-existing เดิม)
    - **Round 8 (2026-08-08, 7-item punch list — batch 1 ของ 3, item 1/3/4):** ลุงเยาะขอ 7 รายการแก้ไข UI พร้อมกัน สั่งให้เรียงตามความยาก ทำง่ายก่อน — เริ่ม 3 ข้อแรก: (1) ไพ่มือ P1 ต้องอยู่เหนือชื่อ/Avatar อย่างน้อย 30px (`GrandmasterTableView.tsx`'s `renderSeat` จัดลำดับใหม่สำหรับ local seat + `localHandGap:{marginBottom:30}`) · (3) ปุ่มต้องเปลี่ยนสีตอนกด ไม่ใช่แค่ opacity/scale (ใช้ `Pressable`'s function-as-children `{({pressed}) => ...}` เพื่อส่ง pressed-state เข้า child `Text` ด้วย ไม่ใช่แค่ style ของ `Pressable` เอง — gold `#FFD76A` bg + `#0F2418` text ตอนกด — ใช้ร่วมกันทั้ง `GrandmasterTableView.tsx` และ `ArenaOverlays.tsx`) · (4) ไพ่กองกลางกอง 3 ใบที่ 2 (ยังไม่หงาย) ต้องโชว์หลังไพ่บนจอด้วยให้รู้ว่ามี 2 ใบ (`arenaProjection.ts`'s `communityCards.pile3` เดิมส่งแค่ใบเดียวก่อนหงาย เปลี่ยนเป็นส่งครบ 2 ช่องเสมอ ใบที่ยังไม่เปิดส่งเป็น `''` placeholder ให้ client fallback เป็นหลังไพ่เอง)
    - **Round 9 (2026-08-08, batch 2 ของ 3, item 5/2):** (5) ไพ่ประมูลต้องเห็นครบตลอดช่วงประมูล ไม่ใช่แค่คนกำลังบิด — field เดิม `auction` gate เฉพาะคนกำลัง bid ตาตัวเอง (ถูกต้องสำหรับปุ่มบิด แต่ผิดสำหรับการโชว์ไพ่) จึงเพิ่ม field ใหม่แยกต่างหาก `auctionDisplay` (ไม่ gate ด้วย `viewerPending`) ให้ทุกคนเห็นไพ่เปิดจริง 1 ใบ + หลังไพ่ปิด 2 ใบตลอดช่วงประมูล — ไพ่เปิดอยู่ซ้าย ไพ่ปิดอยู่ขวาตามที่ขอ · (2) เมื่อกองไหนจบ ไพ่ของทุกคน (รวม Fold/Foul ตามที่ลุงเยาะยืนยัน "จำนวนไพ่บนจอทั้งหงายคว่ำต้องครบ") ต้องคว่ำซ้อนใต้ไพ่กองกลางของกองนั้น offset +10px/ใบ — เพิ่ม field `pilesResolved:{pile1,pile2,pile3}` (true ทันทีที่กอง resolve คงอยู่ตลอดเกม) ให้ client รู้ว่าต้องเริ่มคว่ำกองไหนแล้ว
    - **Round 10 (2026-08-08, batch 3 ของ 3, item 6/7 — Performance Score + Win History):** สองข้อสุดท้าย (ที่ยากที่สุดตามที่ลุงเยาะเรียงไว้) — Arena ไม่เคยมี concept "ใครชนะโต๊ะโดยรวม" มาก่อนเลย มีแต่ผู้ชนะแต่ละกอง เพิ่ม `resolveArenaTableWinner()` ใหม่ใน `arenaProjection.ts` (เทียบ `endingCrest` ของทุก actor ทั้งโต๊ะ human/AI หาอันดับ 1 จริง — export ใช้ร่วมกันทั้งฝั่ง display คือ `psGained` บนหน้าผลสรุป และฝั่งเขียน DB ใน `arenaSocket.ts` กันสองจุดตัดสินผู้ชนะไม่ตรงกัน) พอร์ต pattern เดียวกับ `highNobleMultiEngine.ts`'s `finalWinner` reduce · (6) PS: ลุงเยาะสั่งเลขตรงๆ "บวกให้สูงกว่า High Noble อีก +2 ทุกค่า" — เพิ่ม `grandmasterWin:7`/`grandmasterMonarchWin:12`/`grandmasterNotWinNonNegative:4`/`grandmasterNegative:2` ใน `gameConfig.ts`'s `psConfig` (**เป็นการบวกอิสระ ไม่ใช่ x2** — ต่างจากกฎ `monarchMultiplier` เดิมของ Tier อื่นที่ Monarch เสมอเป็น x2 ของค่าชนะปกติ ตั้งใจบันทึกไว้กันงงว่าทำไม 7×2≠12 · ผลข้างเคียงที่สังเกตได้: Grandmaster เป็น Tier แรกที่ net-negative ได้ PS +2 แทน 0 เหมือน Tier อื่น — เป็นไปตามคำสั่งลุงเยาะตรงๆ ไม่ใช่บั๊ก) `psEngine.ts`'s `AwardPerformanceScoreInput.tier` ขยายรับ `'grandmaster'` · isMonarchMatch ใช้กับทั้ง Monarch และ Soren (สองบอสหายากของ Arena) · โชว์ผลเป็น field ใหม่ `result.psGained` ใน `arenaProjection.ts`/`arenaClientTypes.ts` render แถว "PS +N" ต่อจาก "NET CROWN" ใน `ArenaOverlays.tsx`'s result modal เดิม · (7) Win History: สำรวจแล้วพบว่ามีระบบพร้อมอยู่แล้วไม่ต้องสร้างใหม่ — `matchWinsService.ts`/`match_wins` table (migration 009 รันแล้ว) ใช้ซ้ำกับทุก Tier อื่น ขยาย `MatchWinTier` รับ `'grandmaster'` เฉยๆ (**ตั้งใจไม่ใช้ `matchStatsService.ts`** ที่ผูกกับ Token economy debt-recovery/XP/streak แน่นเกินไป ไม่เข้ากับ Crest economy แยกของ Arena) · `isTripleSweep` ใช้ `PlayerResultBreakdown.sweepJackpot > 0` ที่มีอยู่แล้วโดยไม่ต้องสร้าง tracking ใหม่ · `bestHand: null` เป็น scope cut ที่ตั้งใจ (Arena ไม่มี running "best hand this match" ต่อ actor เหมือน High Noble's `state.bestHandThisMatch`) · เรียกทั้งคู่ใน `arenaSocket.ts`'s `persistSettlement()` (guard `snapshot.completed && !finalizedMatchIds.has(...)` เดิมที่กันยิงซ้ำต่อแมตช์ อยู่แล้ว ต่อจาก `persistMatchLog` เดิม) · เทสใหม่ `arenaTableWinner.test.ts` (`resolveArenaTableWinner` ตรงกับ max-endingCrest ที่หาด้วยมือหลาย seed จริง ผ่าน `playMatch()` เต็มแมตช์ + `psGained` ตรงสูตรทุก viewer) ขยาย `psEngine.test.ts` ครบ 4 branch ใหม่ · Full suite 627/629 ผ่าน (2 fail pre-existing เดิมเหมือนทุกรอบที่ผ่านมา — `vipPlusAccess.test.ts`/`hnGracePeriod.test.ts` ไม่เกี่ยวกับงานนี้)

**Deferred to v1.1:** XP/Leveling, Last Boss UI (Tier S+ / Sovereign playable table — ยืนยันมติอีกครั้ง 2026-08-14 เพราะเวลาก่อน Launch ต.ค. 2026 จำกัด ดูรายละเอียดที่ pending #11), Social, Push Notifications, Lottie, iOS

---

## 🔧 Workflow Rules

1. **Git:** ทำงานบน branch แยกเสมอ ห้าม commit ตรงเข้า main — ลุง review diff ก่อน merge
2. **SQL:** เขียน SQL ให้ลุงรันบน Supabase dashboard เอง — ห้าม execute SQL ตรง
3. **Assets:** boss art `boss_[key].png` (512×640) + avatar 256×256 ที่ `assets/bosses/` | card back `card_back_[skin_key].png` (360×504 @2x)
4. **เอกสาร:** pattern `TriplePoker_[Topic]_v[X]_[Y].md` — เอกสาร canon อยู่ที่ `docs/`
5. ก่อนรัน bash ที่เกี่ยวกับ path ให้ confirm path จริงก่อน
6. Tests ต้องผ่านครบก่อนถือว่างานเสร็จ — **baseline ล่าสุด ณ 2026-08-08: 595 passed / 597 total (67 suites)** ห้ามทำให้ตัวที่ผ่านอยู่ตก · 2 fail ที่เหลือ (`vipPlusAccess.test.ts`/`vipPlusMatchEngine.test.ts`) อยู่ในไฟล์ VIP Plus 5-player ที่ยังไม่ track (`git status` ขึ้น `??`) เป็นงานคู่ขนานแยกต่างหาก ไม่ใช่ Grandmaster/Tier ที่เอกสารนี้คุม — ต้นเหตุคือ `.env`'s `VIP_PLUS_5P_ENABLED=true` ในเครื่อง dev เปิดพฤติกรรม feature flag ที่โค้ดคาดว่าปิดอยู่ ไม่ใช่บั๊กจริงในโค้ด ไม่ต้องไล่แก้ตามกติกาข้อ 7 · baseline เดิม 2026-07-30 (298/298) ล้าสมัยแล้ว (เพิ่มเทสจาก Grandmaster หลายรอบ) · baseline 2026-07-25 (232/239) เก่ากว่านั้นอีก ดู pending #4 · เลข "197/197" เดิมเป็นตัวเลขที่ไม่เคยตรงกับของจริง · **วิธีรันบน Windows: `node node_modules\jest\bin\jest.js`** (ไม่มี `.bin/jest.cmd` เพราะ node_modules ถูก install จากฝั่ง WSL — `npx jest` จะขึ้น "not recognized")

## 🎨 Official Theme (WebsiteTheme_Spec_v1_0)

- BG: `#0F2418` / `#163A25` / `#1C4830` | Gold: `#FFD76A` / `#FFC857`
- Green Highlight: `#8DFFB5` | Red: `#FF6B6B` | Text: `#F5F2E8` / `#C8C4B0` / `#7A7A6A`
- Border: `#2A4A34` / `#3A5A44` | Fonts: Cinzel (heading) / Inter (body) / JetBrains Mono (ตัวเลข/token)
